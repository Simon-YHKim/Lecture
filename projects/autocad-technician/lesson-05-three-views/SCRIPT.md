# SCRIPT — AutoCAD Technician 5차시 · 제3각법 3뷰와 반복

**Part:** `EDU-IB-02 아이들러 풀리 브래킷`<br>
**Checkpoint:** `L04_FEATURES → L05_VIEWS`<br>
**Voice:** 사용자 직접 녹음<br>
**Voice direction:** 뷰를 옮겨 다니는 차시이므로 「정면도에서」 「평면도로」 같은 위치를 말할 때 한 박자 쉬고 이어 간다. 질문은 답을 재촉하지 말고 한 박자 쉬었다 이어 간다.

> 목표 길이 38분 27초 — `SCRIPT.md` 에서 계산된 값입니다. 실제 음성이 들어오면 단어 타임코드가 최종 기준이 됩니다.
> 괄호 안 표기는 그 순간 화면에서 밝아지는 요소를 가리킵니다. 읽지 않습니다.

## Line 1 — 타이틀 (Frame 1)

**Time:** 0:00–0:12

    (무음)

    5차시, 제3각법 3뷰와 반복입니다.

## Line 2 — 오늘 그릴 것 (Frame 2)

**Time:** 0:12–1:51

    지난 시간까지 정면도 하나를 완성했습니다.
    오늘은 그 정면도에서 뷰를 두 개 더 뽑습니다.
    새로 재는 값이 많을까요. 거의 없습니다.
    이미 그어 놓은 선에서 받아옵니다.

    세 뷰는 시험에서만 쓰는 형식이 아닙니다.
    작업을 편하게 할 치구를 제안할 때도 이렇게 그립니다.
    물건 하나를 남에게 설명하는 최소 단위입니다.

    (1번 카드 · 투상선 긋기) 먼저 보조선을 뽑습니다.
    정면도의 모서리와 원에서 뽑습니다.
    위로 올리고 오른쪽으로 긋습니다.
    이 선은 도면에 남지 않습니다.
    폭과 높이를 손으로 다시 재지 않으려고 긋습니다.
    다 쓰고 나면 지웁니다.

    (2번 카드 · 평면도 만들기) 정면도 위에 평면도를 만듭니다.
    가로는 정면도에서 그대로 받아온 120입니다.
    세로는 두께입니다.
    판은 12, 보스가 있는 자리는 20.
    두께는 정면도에 없던 값입니다.
    그래서 여기서 처음 나타납니다.

    (3번 카드 · 우측면도 만들기) 정면도 오른쪽에 우측면도를 만듭니다.
    세로는 정면도의 높이 90을 그대로 받아옵니다.
    가로는 다시 두께입니다.
    그 두께는 평면도에서 45도 선을 거쳐 옵니다.

    (4번 카드 · 숨은선 넣기) 축 구멍과 탭, 장공은 재료 속에 있습니다.
    평면도와 우측면도에서는 보이지 않습니다.
    안 그리면 없는 것이 됩니다.
    실선으로 그리면 보이는 모서리가 됩니다.
    그래서 점선으로 그립니다.

## Line 3 — 세 뷰가 서로를 붙듭니다 (Frame 3)

**Time:** 1:51–4:47

    손을 대기 전에 네 가지를 먼저 정리합니다.
    이걸 모르고 그리면 선은 다 맞는데 배치가 틀립니다.

    (1번 카드 · 정면도를 먼저 정한다) 세 뷰는 정면도부터 정합니다.
    무엇을 정면도로 삼을지에는 기준이 있습니다.
    첫째, 형상이 가장 잘 드러나는 면입니다.
    이 부품이면 베이스와 목과 보스가 한눈에 보이는 면입니다.
    둘째, 부품이 실제로 놓이거나 조립되는 자세입니다.
    이 부품은 프레임 수직면에 볼트 두 개로 붙습니다. 그 자세로 봅니다.
    셋째, 숨은선이 가장 적게 나오는 방향입니다.
    시험에서는 3차원 그림을 받습니다. 이걸 먼저 정하셔야 합니다.
    정하고 나면 나머지 두 뷰는 따라옵니다.
    제3각법은 본 방향에 그 뷰를 놓습니다.
    위에서 봤으면 위에 놓습니다.
    오른쪽에서 봤으면 오른쪽에 놓습니다.
    그래서 정면도 위가 평면도입니다.
    정면도 오른쪽이 우측면도입니다.
    제1각법은 반대로 놓습니다.
    위에서 본 것을 아래에 두는 방식입니다.
    같은 부품인데 배치가 다르면 어떻게 될까요.
    읽는 사람이 위아래를 반대로 이해합니다.
    현장에서 받는 설비 조립도도 대부분 제3각법입니다.
    스페어 파트 도면을 급히 확인할 때 이 배치를 알아야 합니다.
    우리 도면도 제3각법입니다.

    (2번 카드 · 투상선이 왜 필요한가) 같은 값을 두 번 재지 않기 위해서입니다.
    정면도의 폭 120이 그대로 평면도의 폭입니다.
    정면도의 높이 90이 그대로 우측면도의 높이입니다.
    눈으로 맞추면 조금씩 어긋납니다.
    그 어긋남은 나중에 치수를 넣을 때 드러납니다.
    투상선을 걸어 두면 어긋날 수가 없습니다.
    뷰끼리 붙들어 매는 겁니다.

    (3번 카드 · 숨은선은 무엇을 그리는가) 재료에 가려서 안 보이는 모서리를 그립니다.
    판단은 세 갈래입니다.
    보이면 외형선입니다.
    있는데 안 보이면 숨은선입니다.
    아예 없으면 안 그립니다.
    축 구멍은 앞뒤로 뚫려 있습니다.
    그런데 위에서 내려다보면 재료에 가립니다.
    그래서 평면도에서는 점선입니다.

    (4번 카드 · 45도 선으로 잇기) 여기가 오늘 제일 헷갈리는 자리입니다.
    깊이는 정면도에 없습니다.
    평면도에서 처음 정해집니다.
    그 깊이를 우측면도로 어떻게 옮길까요.
    45도 선이 그 장치입니다.
    평면도에서 오른쪽으로 나간 선이 45도 선에서 꺾입니다.
    꺾여서 아래로 내려옵니다.
    평면도의 세로 거리가 우측면도의 가로 거리가 됩니다.
    45도이기 때문에 두 거리가 같습니다.
    자로 재서 옮기면 틀립니다.
    이 선으로 옮기면 틀릴 수가 없습니다.

## Line 4 — 도면 위에서 (Frame 4)

**Time:** 4:47–7:39

    이제 이 부품에서 여섯 자리를 짚습니다.
    방금 본 규칙이 실제로 어디에 붙는지 봅니다.

    (1행 · 평면도 판 두께 12) 평면도의 가로는 투상선이 정해 줍니다.
    정면도에서 위로 올린 선입니다.
    왼쪽 끝과 오른쪽 끝, 120 그대로입니다.
    남는 것은 세로 하나뿐입니다.
    그게 두께 12입니다.
    평면도에서 새로 정하는 값은 사실상 두께뿐입니다.

    (2행 · 평면도 보스 자리 20) 보스는 앞으로 8 나옵니다.
    그래서 그 자리만 12에 8을 더한 20입니다.
    가로로 어디까지가 그 자리일까요.
    보스 원이 정합니다.
    지름 56, 중심이 왼쪽에서 60입니다.
    그러니 32에서 88까지입니다.
    이 두 값은 계산해서 입력하지 않습니다.
    보스 원의 좌우 사분점에서 투상선을 올려 받습니다.

    (3행 · 우측면도 높이 90) 우측면도의 세로도 투상선이 정해 줍니다.
    정면도에서 오른쪽으로 그은 선입니다.
    바닥 0, 베이스 윗면 16입니다.
    보스 아래 34, 맨 위 90입니다.
    보스 중심이 62이고 반지름이 28입니다.
    62에서 28을 빼면 34입니다.
    더하면 90입니다.
    부품의 전체 높이 90은 보스 윗면이 만든 값입니다.

    (4행 · 축 구멍 숨은선) 축 구멍 Ø25는 앞뒤로 관통합니다.
    평면도에서도 우측면도에서도 두께 전체를 지납니다.
    보스 자리에 있으니 20 전체입니다.
    자리는 중심에서 지름의 절반인 12.5씩 벌어진 곳입니다.
    평면도에서는 중심 60이니 47.5와 72.5입니다.
    우측면도에서는 중심 62이니 49.5와 74.5입니다.

    (5행 · 탭 깊이 10 숨은선) 탭은 관통이 아닙니다.
    보스 앞면에서 10까지만 들어가고 멈춥니다.
    그래서 점선이 두께 한가운데에서 끝납니다.
    앞에서 10 들어가면 뒤에 10이 남습니다.
    가로 자리 네 곳은 계산으로 나옵니다.
    피치원 반지름은 22입니다. 첫 구멍은 45도 자리입니다.
    22 곱하기 코사인 45도, 약 15.6입니다.
    중심 60에서 빼고 더하면 44.4와 75.6입니다.
    위아래 두 개가 같은 가로 자리에 겹칩니다.
    그래서 평면도에서는 두 자리로만 나타납니다.

    (6행 · 장공 숨은선) 장공도 관통입니다.
    다만 보스가 없는 자리에 있습니다.
    그래서 두께 12 구간에만 점선이 들어갑니다.
    축 구멍은 보스 앞면부터 시작합니다.
    장공은 판 앞면부터 시작합니다.
    시작하는 깊이가 8만큼 다릅니다.
    이걸 맞추지 않으면 어떻게 될까요.
    판 앞으로 튀어나온 구멍이 됩니다.

## Line 5 — 세 뷰 만들기 · DEMO-01 화면 녹화 (Frame 5)

**Time:** 7:39–32:09

> 이 구간은 화면 녹화입니다. 아래 16단계를 **순서대로 빠짐없이** 수행하면서 말합니다.
> 백틱 안은 실제로 타이핑하는 값이고, 클릭이라고 쓴 곳은 객체 스냅으로 점을 잡는 자리입니다.
> 스냅이 안 붙으면 F3으로 객체 스냅이 켜져 있는지 먼저 확인합니다.

### 1단계 — 지난 파일 열기

    AutoCAD를 켜고 `OPEN` 입력하고 엔터.
    지난 시간에 저장한 `EDU-IB-02_L04_FEATURES` 파일을 고릅니다.
    새로 만들지 않습니다.
    오늘은 이 정면도 위에 뷰를 얹는 시간입니다.
    지난 선이 그대로 있어야 합니다.

    `Z` 엔터, `A` 엔터로 화면을 맞춥니다.
    정면도가 도면틀 안 왼쪽 아래에 있는지 봅니다.
    위쪽과 오른쪽에 빈자리가 남아 있어야 합니다.
    평면도와 우측면도가 들어갈 자리입니다.

### 2단계 — 투상선을 그을 레이어 정하기

    화면 위 레이어 목록을 열고 `치수선`을 고릅니다.
    투상선은 여기에 긋습니다.

    외형선에 그으면 안 됩니다.
    지우는 것을 잊으면 흰 실선이 그대로 남습니다.
    그러면 진짜 모서리로 읽힙니다.
    중심선이나 숨은선도 도면에 남는 선입니다.
    섞이면 나중에 구분이 안 됩니다.
    치수선 레이어에는 아직 아무것도 없습니다.
    색이 보라라서 한눈에 구분됩니다.
    마지막에 이 레이어만 껐다 켜 봅니다.
    투상선이 다 지워졌는지 바로 확인됩니다.

### 3단계 — 정면도에서 수직 투상선 올리기

    `XL` 입력하고 엔터. 구성선 명령입니다.
    옵션을 물어보면 `V` 입력하고 엔터.
    수직선만 그립니다.
    이제 클릭하는 점마다 수직선이 하나씩 생깁니다.
    무한히 긴 선입니다.

    정면도 왼쪽 아래 모서리를 클릭합니다.
    끝점 스냅입니다. 왼쪽 끝 0입니다.
    오른쪽 아래 모서리를 클릭합니다. 오른쪽 끝 120입니다.
    왼쪽 모따기의 위쪽 끝점을 클릭합니다. 5입니다.
    오른쪽 모따기의 위쪽 끝점을 클릭합니다. 115입니다.
    모따기도 평면도에 나옵니다.
    위에서 내려다보면 그 5씩이 비스듬히 깎여 보입니다.
    보스 원의 왼쪽 사분점을 클릭합니다. 32입니다.
    보스 원의 오른쪽 사분점을 클릭합니다. 88입니다.
    보스 원의 중심을 클릭합니다. 60입니다.
    중심선을 그을 때 씁니다.
    엔터를 눌러 명령을 끝냅니다.

    사분점은 원의 위아래 좌우 네 점입니다.
    32와 88을 왜 계산해서 넣지 않을까요.
    입력하는 순간 오타가 들어올 수 있습니다.
    원이 조금이라도 옮겨져 있으면 그 오차가 그대로 남습니다.
    원에서 직접 받으면 그럴 일이 없습니다.

### 4단계 — 평면도의 깊이선 세 개

    `XL` 엔터. `H` 엔터. 이번에는 수평선입니다.
    보스 원의 위쪽 사분점을 클릭합니다.
    정면도 맨 위, 높이 90을 지나는 수평선이 생겼습니다.
    엔터로 끝냅니다.

    `O` 입력하고 엔터. 간격 띄우기 명령입니다.
    간격을 물어보면 `30` 입력하고 엔터.
    방금 그은 수평선을 클릭합니다.
    그 위쪽 아무 데나 클릭합니다.
    30 위에 선이 하나 생깁니다.
    평면도의 맨 아래 선, 보스 앞면입니다.
    엔터로 끝냅니다.

    30은 정해진 값이 아닙니다.
    뷰와 뷰 사이를 얼마나 띄울지는 그리는 사람이 정합니다.
    치수가 들어갈 자리는 남깁니다.
    세 뷰가 도면틀 안에 균형 있게 들어가면 됩니다.
    이 도면에서는 30으로 갑니다.

    `O` 엔터. `8` 엔터.
    방금 만든 선을 클릭하고 위쪽 클릭. 판 앞면입니다.
    보스가 앞으로 8 나와 있으니 판 앞면은 그보다 8 뒤입니다.
    `O` 엔터. `12` 엔터.
    판 앞면 선을 클릭하고 위쪽 클릭. 뒷면입니다.
    판 두께 12입니다.

    이 명령은 현장에서도 자주 씁니다.
    설비 레이아웃에서 벽체 두께를 잡을 때가 그렇습니다.
    통로 여유를 확인할 때도 같은 방식입니다.
    선 하나를 정해진 거리만큼 밀어 보는 겁니다.

    맨 아래에서 맨 위까지 8 더하기 12, 20입니다.
    보스가 있는 자리의 두께입니다.
    평면도는 위쪽이 부품의 뒷면, 아래쪽이 앞면입니다.
    정면도에서 우리 쪽을 향하던 면이 있습니다.
    그 면이 평면도에서는 아래로 옵니다.

### 5단계 — 평면도 외형 그리기

    레이어 목록에서 `외형선`으로 바꿉니다.
    여기서부터는 도면에 남는 선입니다.

    `L` 엔터.
    보스 앞면 선과 왼쪽 투상선의 교차점을 클릭합니다.
    같은 선과 오른쪽 투상선의 교차점을 클릭합니다.
    엔터로 끝냅니다.
    폭 전체를 가로지르는 선 하나가 생겼습니다.
    같은 방법으로 판 앞면 선 위에 하나 긋습니다.
    뒷면 선 위에도 하나. 가로는 세 줄입니다.

    이번에는 세로입니다. `L` 엔터.
    왼쪽 투상선과 보스 앞면 선의 교차점을 클릭합니다.
    같은 투상선과 뒷면 선의 교차점을 클릭합니다. 엔터.
    같은 방법으로 5 자리, 32 자리, 88 자리, 115 자리, 120 자리에도 하나씩.
    세로는 여섯 줄입니다.
    5와 115가 모따기 자리입니다.
    그 바깥은 비스듬히 깎인 면이라 여기서 면이 한 번 꺾입니다.

    지금은 격자처럼 보입니다.
    필요 없는 구간이 섞여 있습니다.
    다음 단계에서 잘라냅니다.

### 6단계 — 평면도 다듬기

    `TR` 입력하고 엔터. 잘라내기 명령입니다.
    자를 경계를 묻는 프롬프트가 나옵니다.
    엔터를 한 번 더 눌러 화면의 모든 객체를 경계로 잡습니다.
    버전에 따라 곧바로 자를 구간을 물어보기도 합니다.
    어느 쪽이든 그다음은 지울 구간을 클릭하면 됩니다.

    보스 앞면 선에서 32 왼쪽 구간을 클릭합니다. 사라집니다.
    88 오른쪽 구간도 클릭합니다.
    보스 앞면은 32에서 88 사이에만 있어야 하기 때문입니다.

    판 앞면 선에서 32와 88 사이 구간을 클릭합니다.
    그 구간은 보스가 앞을 덮고 있습니다.
    그래서 판 앞면이 드러나지 않습니다.

    왼쪽 세로선에서 맨 아래 8 구간을 클릭합니다.
    오른쪽 세로선도 같은 구간을 클릭합니다.
    판의 양 끝에는 보스가 없습니다.
    그만큼 앞으로 나와 있지 않습니다.

    32 세로선에서 판 앞면보다 위쪽 구간을 클릭합니다.
    88 세로선도 같은 구간을 클릭합니다.
    보스의 단차는 앞으로 나온 8 구간에만 있습니다.
    엔터로 끝냅니다.

    남은 모양을 봅니다.
    가로 120, 세로 12의 긴 사각형입니다.
    그 가운데에서 폭 56짜리 부분이 아래로 8 더 나왔습니다.
    이게 이 부품을 위에서 내려다본 모양입니다.

    설비 레이아웃 도면도 이 평면도와 같은 시점입니다.
    설비가 들어갈 자리와 동선을 위에서 내려다보고 잡습니다.
    간섭이 나는 자리도 대개 여기서 먼저 보입니다.

### 7단계 — 우측면도 자리와 45도 선

    레이어를 다시 `치수선`으로 바꿉니다.
    여기서부터 다시 보조선입니다.

    `O` 엔터. `30` 엔터.
    정면도 오른쪽 끝에서 올린 수직 투상선을 클릭합니다.
    오른쪽을 클릭합니다. 엔터로 끝냅니다.
    우측면도의 맨 왼쪽 선입니다.
    여기가 보스 앞면입니다.
    우측면도는 왼쪽이 앞면, 오른쪽이 뒷면입니다.
    정면도에 가까운 쪽이 앞면입니다.

    `XL` 엔터. `A` 입력하고 엔터.
    각도를 물어봅니다. `45` 입력하고 엔터.
    통과점을 찍어야 합니다.
    방금 만든 세로선과 평면도 맨 아래 선의 교차점입니다.
    클릭하고 엔터로 끝냅니다.
    두 뷰의 앞면끼리 만나는 점입니다.
    이 점을 지나야 깊이가 그대로 옮겨집니다.

    `XL` 엔터. `V` 엔터.
    45도 선과 평면도 판 앞면 선의 교차점을 클릭합니다.
    45도 선과 뒷면 선의 교차점도 클릭합니다. 엔터.

    세로선 두 개가 더 생겼습니다.
    우측면도 왼쪽 선에서 8, 그리고 20 떨어진 자리입니다.
    평면도에서 위로 8, 20이던 거리입니다.
    그 거리가 우측면도에서는 오른쪽으로 8, 20이 됐습니다.
    재지 않고 옮겼습니다.

### 8단계 — 정면도에서 수평 투상선 긋기

    `XL` 엔터. `H` 엔터.
    정면도 왼쪽 아래 모서리를 클릭합니다. 바닥 0입니다.

    다음 통과점에서 `FROM` 입력하고 엔터.
    기준점을 물어봅니다.
    같은 왼쪽 아래 모서리를 클릭합니다.
    `@0,16` 입력하고 엔터.
    바닥에서 16 올라간 자리에 수평선이 생깁니다.
    베이스 윗면입니다.
    `FROM`은 잡은 점에서 상대좌표만큼 떨어진 점을 찍는 기능입니다.
    도면에 점이 없는 자리를 잡을 때 씁니다.

    오른쪽 세로변의 위쪽 끝점을 클릭합니다. 11입니다.
    모따기가 시작되는 높이입니다. 16에서 5를 뺀 값입니다.
    보스 원의 아래쪽 사분점을 클릭합니다. 34입니다.
    보스 원의 중심을 클릭합니다. 62입니다.
    중심선을 그을 때 씁니다.
    왼쪽 장공의 왼쪽 끝원 중심을 클릭합니다. 8입니다.
    이것도 중심선용입니다.
    엔터로 끝냅니다.

    맨 위 90은 4단계에서 이미 그었습니다.
    그 선이 오른쪽으로도 뻗어 있습니다.
    다시 그을 필요가 없습니다.
    구성선이 무한히 긴 이유가 여기 있습니다.
    한 번 그으면 세 뷰를 다 지나갑니다.

### 9단계 — 우측면도 외형 그리기

    레이어를 `외형선`으로 바꿉니다.

    `L` 엔터.
    이번에는 잘라낼 것 없이 교차점을 따라 그립니다.
    판 앞면 세로선과 바닥선의 교차점 클릭.
    뒷면 세로선과 바닥선의 교차점 클릭.
    뒷면 세로선과 90선의 교차점 클릭.
    보스 앞면 세로선과 90선의 교차점 클릭.
    보스 앞면 세로선과 34선의 교차점 클릭.
    판 앞면 세로선과 34선의 교차점 클릭.
    마지막으로 `C` 입력하고 엔터.
    시작점까지 닫으면서 명령이 끝납니다.

    아래쪽은 폭 12입니다.
    34부터 위쪽은 폭 20입니다.
    보스가 34에서 90까지 앞으로 8 나와 있는 모양입니다.

    `L` 엔터.
    판 앞면 세로선과 16선의 교차점 클릭.
    뒷면 세로선과 16선의 교차점 클릭. 엔터.
    베이스 윗면입니다.
    오른쪽에서 보면 베이스 끝면의 윗변이 이 가로선입니다.

    `L` 엔터. 11선에도 같은 방법으로 하나 긋습니다.
    판 앞면 세로선과 11선의 교차점을 클릭합니다.
    뒷면 세로선과 11선의 교차점을 클릭합니다. 엔터.
    모따기가 시작되는 자리입니다.
    오른쪽에서 보면 11까지가 옆면입니다. 그 위 5는 비스듬한 면입니다.
    두 면이 만나는 모서리가 이 가로선입니다.

    5단계에서는 격자를 그려 놓고 잘라냈습니다.
    여기서는 교차점을 따라 곧장 이어 그렸습니다.
    잘라낼 구간이 많으면 앞의 방법이 빠릅니다.
    윤곽이 한 줄로 이어지면 뒤의 방법이 빠릅니다.

### 10단계 — 중심선 세 개

    레이어를 `중심선`으로 바꿉니다.

    `L` 엔터. 첫 번째 점에서 `FROM` 입력하고 엔터.
    기준점은 60 수직 투상선입니다.
    그 선과 평면도 보스 앞면 선의 교차점을 잡습니다.
    클릭한 다음 `@0,-5` 입력하고 엔터.
    두 번째 점은 `@0,30` 입력하고 엔터. 엔터로 끝냅니다.
    평면도를 세로로 가로지르는 축 중심선입니다.
    두께 20에 위아래로 5씩 더해 30입니다.

    `L` 엔터. `FROM` 엔터.
    기준점은 62 수평 투상선입니다.
    그 선과 우측면도 보스 앞면 세로선의 교차점을 잡습니다.
    `@-5,0` 엔터, `@30,0` 엔터, 엔터.
    우측면도의 축 중심선입니다.

    `L` 엔터. `FROM` 엔터.
    기준점은 8 수평 투상선입니다.
    그 선과 우측면도 판 앞면 세로선의 교차점을 잡습니다.
    `@-5,0` 엔터, `@22,0` 엔터, 엔터.
    장공의 중심선입니다.
    장공은 두께 12 구간에만 있습니다.
    그래서 12에 5씩 더해 22입니다.

    중심선은 형상 밖으로 조금 나오게 그립니다.
    여기서는 5씩으로 잡았습니다.
    끝을 형상에 딱 맞추면 어떻게 될까요.
    어디가 중심인지 눈에 안 들어옵니다.

### 11단계 — 축 구멍 숨은선 · 평면도

    레이어를 `치수선`으로 바꾸고 `XL` 엔터, `V` 엔터.
    축 구멍 원의 왼쪽 사분점 클릭, 오른쪽 사분점 클릭, 엔터.
    47.5와 72.5입니다.

    레이어를 `숨은선`으로 바꿉니다. `L` 엔터.
    왼쪽 투상선과 평면도 보스 앞면 선의 교차점 클릭.
    같은 투상선과 뒷면 선의 교차점 클릭. 엔터.
    오른쪽 투상선에도 같은 방법으로 하나 더 긋습니다.

    두 줄 다 두께 20 전체를 지나갑니다.
    축 구멍은 앞뒤로 관통합니다.
    그 자리가 보스라서 20이기 때문입니다.
    선이 노란 점선으로 보이는지 확인합니다.
    흰 실선이면 레이어를 안 바꾸고 그린 겁니다.

### 12단계 — 축 구멍 숨은선 · 우측면도

    레이어를 `치수선`으로 바꾸고 `XL` 엔터, `H` 엔터.
    축 구멍 원의 위쪽 사분점 클릭, 아래쪽 사분점 클릭, 엔터.
    74.5와 49.5입니다. 중심 62에서 12.5씩입니다.

    레이어를 `숨은선`으로 바꾸고 `L` 엔터.
    보스 앞면 세로선과 위쪽 투상선의 교차점 클릭.
    뒷면 세로선과 같은 투상선의 교차점 클릭. 엔터.
    아래쪽 투상선에도 하나 더 긋습니다.
    두 줄 다 가로로 20입니다.

    평면도에서 세로 20이던 것이 있습니다.
    우측면도에서는 그게 가로 20입니다.
    같은 구멍을 다른 방향에서 본 것뿐입니다.

### 13단계 — 탭 숨은선 · 평면도

    레이어를 `치수선`으로 바꾸고 `XL` 엔터, `V` 엔터.
    왼쪽 위 탭 원의 왼쪽 사분점과 오른쪽 사분점을 클릭합니다.
    오른쪽 위 탭 원도 같은 두 점을 클릭합니다. 엔터.
    투상선은 네 개입니다.
    아래쪽 탭 두 개는 위쪽 탭과 가로 자리가 같습니다.
    그래서 같은 선에 겹칩니다.

    레이어를 `숨은선`으로 바꾸고 `L` 엔터.
    첫 번째 투상선과 평면도 보스 앞면 선의 교차점을 클릭합니다.
    `@0,10` 입력하고 엔터, 엔터로 끝냅니다.
    나머지 세 개도 같은 방법으로 긋습니다.
    네 줄 모두 길이 10입니다.

    보스 앞면에서 10까지만 들어가고 멈춥니다.
    뒷면까지가 20이니 10이 남습니다.
    점선이 허공에서 끝나 보입니다. 맞습니다.
    구멍이 거기서 끝나기 때문입니다.

    화면을 확대해서 한 가지를 봅니다.
    탭 숨은선의 안쪽 줄과 축 구멍 숨은선입니다.
    두 줄이 붙지 않았는지 확인합니다.
    M5는 지름 5이니 반지름 2.5입니다.
    44.4에 2.5를 더하면 46.9입니다.
    축 구멍 숨은선은 47.5입니다.
    0.6밖에 안 떨어져 있습니다.
    축소해서 보면 한 줄로 보입니다.

### 14단계 — 탭 숨은선 · 우측면도와 대칭 복사

    레이어를 `치수선`으로 바꾸고 `XL` 엔터, `H` 엔터.
    위쪽 탭 원의 위쪽 사분점과 아래쪽 사분점을 클릭하고 엔터.

    레이어를 `숨은선`으로 바꾸고 `L` 엔터.
    보스 앞면 세로선과 위쪽 투상선의 교차점 클릭.
    `@10,0` 입력하고 엔터, 엔터.
    아래쪽 투상선에도 같은 방법으로 하나.
    두 줄이 됐습니다.

    이제 아래쪽 탭입니다.
    다시 그리지 않고 대칭 복사합니다.
    `MI` 입력하고 엔터.
    방금 그은 두 줄을 클릭해서 선택하고 엔터.
    대칭선의 첫 번째 점을 물어봅니다.
    우측면도 축 중심선의 왼쪽 끝점을 클릭합니다.
    두 번째 점은 같은 중심선의 오른쪽 끝점입니다.
    원본을 지울지 물어봅니다. `N` 입력하고 엔터.
    지우지 않습니다.

    탭 네 개는 첫 구멍이 45도입니다.
    거기서 90도씩 돌아갑니다.
    그래서 위 두 개가 같은 높이입니다.
    아래 두 개도 같은 높이입니다.
    우측면도에는 높이가 두 자리만 나타납니다.
    그 두 자리는 중심 62를 기준으로 위아래 대칭입니다.
    대칭인 것을 대칭 명령으로 만들면 값을 계산할 일이 없습니다.
    계산해 넣지 않으니 어긋날 일도 없습니다.

### 15단계 — 장공 숨은선

    레이어를 `치수선`으로 바꾸고 `XL` 엔터, `V` 엔터.
    왼쪽 장공에서 왼쪽 끝원의 왼쪽 사분점을 클릭합니다.
    오른쪽 끝원의 오른쪽 사분점을 클릭합니다. 엔터.
    24와 46입니다.
    끝원 중심이 29와 41이고 반지름이 5입니다.
    29에서 5를 빼고 41에 5를 더한 값입니다.

    레이어를 `숨은선`으로 바꾸고 `L` 엔터.
    왼쪽 투상선과 평면도 판 앞면 선의 교차점 클릭.
    같은 투상선과 뒷면 선의 교차점 클릭. 엔터.
    오른쪽 투상선에도 하나 더.
    두 줄 모두 길이 12입니다.

    보스 앞면 선까지 내려가면 안 됩니다.
    장공은 보스가 없는 베이스에 있습니다.
    그 자리 두께는 12입니다.
    축 구멍은 보스 앞면부터 시작합니다.
    장공은 판 앞면부터 시작합니다.
    시작 깊이가 8 다릅니다.

    오른쪽 장공은 대칭 복사합니다. `MI` 엔터.
    방금 그은 두 줄을 선택하고 엔터.
    대칭선은 평면도 축 중심선입니다.
    위쪽 끝점 클릭, 아래쪽 끝점 클릭.
    원본을 지울지 물으면 `N` 엔터.
    장공 중심이 35와 85입니다.
    둘 다 60에서 25씩 떨어져 있습니다.
    그래서 대칭이 정확히 맞습니다.

    우측면도로 갑니다.
    레이어를 `치수선`으로 바꾸고 `XL` 엔터, `H` 엔터.
    장공 끝원의 위쪽 사분점과 아래쪽 사분점을 클릭하고 엔터.
    13과 3입니다.
    중심 8에 폭 10의 절반인 5씩입니다.

    레이어를 `숨은선`으로 바꾸고 `L` 엔터.
    판 앞면 세로선과 위쪽 투상선의 교차점 클릭.
    뒷면 세로선과 같은 투상선의 교차점 클릭. 엔터.
    아래쪽 투상선에도 하나 더.
    두 줄 모두 가로 12입니다.
    장공 두 개는 높이가 같습니다.
    그래서 우측면도에서는 한 쌍으로 겹칩니다.
    두 줄이면 충분합니다.

### 16단계 — 투상선 지우고 저장

    레이어를 `외형선`으로 되돌립니다.

    먼저 확인부터 합니다. `LAYER` 엔터.
    치수선 레이어의 전구 아이콘을 눌러 끕니다.
    대화상자를 닫습니다.
    화면에서 투상선이 하나도 남지 않아야 합니다.
    남아 있는 선이 있으면 다른 레이어에 그린 겁니다.
    다시 `LAYER` 엔터로 전구를 켜 둡니다.

    투상선은 오늘 지우지 않고 남겨 둡니다.
    다음 차시에 정리 명령을 배우면서 함께 걷어냅니다.
    전부 구성선으로 그어 두었습니다.
    그때 `QSELECT` 로 객체 유형만 골라 한 번에 지웁니다.
    지금 지워 버리면 어떻게 될까요.
    뷰가 서로 맞는지 다시 확인할 방법이 없어집니다.

    `Z` 엔터, `A` 엔터. 세 뷰를 봅니다.
    정면도 위에 평면도가 있는지 봅니다.
    정면도 오른쪽에 우측면도가 있는지 봅니다.
    평면도의 좌우 끝이 정면도의 좌우 끝과 세로로 맞는지 봅니다.
    우측면도의 위아래 끝이 정면도와 가로로 맞는지 봅니다.
    숨은선이 노란 점선인지 봅니다.
    중심선이 빨간 일점쇄선인지 봅니다.
    점선이 실선처럼 보이면 `LTSCALE` 입력하고 엔터.
    값을 키워서 획과 틈을 벌립니다.
    반대로 점이 너무 듬성듬성하면 값을 줄입니다.
    값을 바꿔도 화면이 그대로면 `RE` 엔터로 재생성합니다.

    `SAVEAS` 입력하고 엔터.
    파일 이름은 `EDU-IB-02_L05_VIEWS`.
    저장을 누릅니다.
    다음 시간에는 이 파일을 열어서 시작합니다.

## Line 6 — 이것만은 확인하고 넘어갑니다 (Frame 6)

**Time:** 32:09–34:20

    자주 나오는 실수 네 가지입니다.
    넷 다 선 자체는 정확합니다.
    자리나 종류가 틀린 경우입니다.
    그래서 그린 사람 눈에는 잘 안 보입니다.

    (1번 카드 · 평면도를 아래에 그렸다) 평면도를 정면도 아래에 그렸다고 합시다.
    그건 제1각법 배치입니다.
    선은 하나도 안 틀렸습니다.
    그런데 도면 전체가 다른 뜻이 됩니다.
    읽는 사람은 배치를 보고 본 방향을 판단하기 때문입니다.
    위에서 본 것은 위에 놓습니다.
    다 그린 뒤에 고치려면 뷰를 통째로 옮겨야 합니다.
    그러니 자리를 잡는 단계에서 확인합니다.

    (2번 카드 · 투상선을 외형선에 그었다) 투상선은 도면에 남지 않는 선입니다.
    외형선 레이어에 그으면 흰 실선이 됩니다.
    나머지 형상과 구분이 안 됩니다.
    지우는 것을 잊으면 어떻게 될까요.
    도면 밖으로 뻗어 나간 모서리가 하나 생깁니다.
    치수선 레이어에 긋습니다.
    끝나면 그 레이어를 잠깐 꺼 봅니다.
    투상선이 전부 사라지는지 확인합니다.
    안 사라지는 선이 있으면 레이어를 잘못 쓴 겁니다.

    (3번 카드 · 숨은선을 외형선으로 그렸다) 레이어를 안 바꾸고 그으면 숨은선이 실선이 됩니다.
    실선은 보이는 모서리라는 뜻입니다.
    그러면 안에 뚫린 구멍이 겉면에 파인 홈처럼 읽힙니다.
    그릴 때마다 레이어 목록을 봅니다.
    그린 뒤에는 색으로 확인합니다.
    노란 점선이면 맞습니다.

    (4번 카드 · 탭을 관통으로 그렸다) 탭 숨은선을 두께 전체로 그으면 안 됩니다.
    도면이 4-M5 깊이 10과 다른 말을 하게 됩니다.
    만드는 사람은 도면을 보고 뚫습니다.
    관통으로 그려져 있으면 뒤까지 뚫습니다.
    뒷면은 프레임에 밀착해야 하는 면입니다.
    거기에 구멍이 납니다.
    스페어 파트를 급히 맡길 때도 이 한 장으로 소통합니다.
    가공집이 보는 것은 우리 말이 아니라 이 선입니다.
    보스 앞면에서 10까지만 긋고 끊습니다.

## Line 7 — 이번 차시와 다음 차시 (Frame 7)

**Time:** 34:20–36:39

    (1 왼쪽) 오늘 한 일을 정리합니다.
    정면도 하나에서 뷰 두 개를 만들었습니다.
    정면도의 모서리와 원 사분점에서 구성선을 뽑았습니다.
    폭과 높이를 그대로 받아왔습니다.
    오늘 새로 정한 값은 두께 12와 보스 자리 20뿐이었습니다.
    평면도는 격자를 그려 놓고 필요 없는 구간을 잘라냈습니다.
    우측면도는 교차점을 따라 한 번에 이어 그렸습니다.
    두 방법을 언제 쓰는지도 봤습니다.
    평면도의 깊이는 45도 선을 거쳐 우측면도로 옮겼습니다.
    재지 않고 옮겼습니다.
    그래서 두 뷰의 두께가 어긋날 수 없습니다.
    숨은선은 셋을 다르게 넣었습니다.
    축 구멍은 관통이라 두께 20 전체입니다.
    탭은 보스 앞면에서 10까지입니다.
    장공은 보스가 없는 자리라 판 두께 12 구간에만 넣었습니다.
    마지막으로 대칭인 것은 대칭 명령으로 만들었습니다.
    우측면도의 아래쪽 탭과 평면도의 오른쪽 장공입니다.
    투상선은 껐다 켜서 확인만 하고 그대로 남겨 두었습니다.
    세 뷰의 정렬을 확인한 뒤 L05_VIEWS 상태로 저장했습니다.

    (2 오른쪽) 다음 시간에는 그려 놓은 것을 정리합니다.
    그리고 제대로 보이게 만듭니다.
    오늘 뷰를 맞추려고 그은 구성선을 한 번에 골라 걷어냅니다.
    잘라내지 못하고 남은 형상선 토막은 TRIM으로 자릅니다.
    경계까지 못 미친 선은 EXTEND로 늘립니다.
    자를 경계가 없는 선은 지웁니다.
    대칭인 형상은 MIRROR로 좌표를 입력해 다시 맞춥니다.
    레이어가 틀린 선은 다시 그리지 않습니다.
    MATCHPROP으로 제자리에 옮깁니다.
    그다음 색상과 선 종류, 선가중치를 ByLayer로 통일합니다.
    중심선과 숨은선의 선 종류 축척은 0.5로 넣습니다.
    LTSCALE로 도면 전체의 파선 간격을 맞춥니다.
    마지막으로 표기 네 가지를 읽고 넘어갑니다.
    표면 거칠기 기호, 기하 공차, 재질, 열처리 지시입니다.
    실제 도면에는 들어가지만 이 과정 실습에서는 그리지 않습니다.
    각각 무엇을 지시하는지만 알고 갑니다.

## Line 8 — 오늘 친 것 (Frame 8)  <!-- generated: keys -->

**Time:** 36:39–38:10

    오늘 친 명령을 한 번에 모아 봅니다. 명령 이름보다 **언제 쓰는지**를 기억하시면 됩니다.
    시험이 끝나도 남는 건 그쪽입니다.

    (1) `OPEN`. 파일을 엽니다. 매 차시 지난 상태에서 이어 그릴 때 씁니다.

    (2) `Z`. ZOOM. 화면 배율을 바꿉니다. 스냅이 어디 붙었는지 봐야 할 때 씁니다.

    (3) `XL`. XLINE. 무한히 뻗는 구성선을 긋습니다. 투상선에 씁니다.

    (4) `O`. OFFSET. 일정 거리만큼 떨어진 같은 모양을 만듭니다. 중심을 다시 찍지 않고 동심원·평행선에 씁니다.

    (5) `L`. LINE. 선을 긋습니다. 낱개로 다뤄야 하는 조각에 씁니다.

    (6) `TR`. TRIM. 경계까지 잘라냅니다. 겹친 선, 튀어나온 토막에 씁니다.

    (7) `C`. CIRCLE. 원을 그립니다. 구멍, 축, 피치원에 씁니다.

    (8) `MI`. MIRROR. 대칭으로 복사합니다. 좌우 대칭 형상에 씁니다.

    (9) `LAYER`. 레이어를 만들고 관리합니다. 선 종류·색상을 개체가 아니라 레이어로 정할 때 씁니다.

    (10) `QSELECT`. 조건에 맞는 객체를 한 번에 고릅니다. 구성선만 골라 지울 때 씁니다.

    (11) `LTSCALE`. 도면 전체의 선 종류 축척을 바꿉니다. 점선이 실선처럼 보일 때 씁니다.

    (12) `SAVEAS`. 새 이름으로 저장합니다. 차시 종료 상태를 남길 때 씁니다.

## Line 9 — 고생하셨습니다 (Frame 9)

**Time:** 38:10–38:27

    (1) 오늘 분량은 여기까지입니다.
    뷰를 나누고 다시 맞추는 일이라 손이 많이 갔습니다.
    고생하셨습니다.

    (2) 다음은 6차시, 편집과 표현입니다.
    그때 다시 뵙겠습니다.

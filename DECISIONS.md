# DECISIONS — AutoCAD 기능사 강좌

append-only. 요약·재작성·삭제 금지. 1줄 = `YY.MM.DD HH:MM · 결정 · 이유 · 뒤집는 조건`.
100KB 에 닿으면 요약하지 않고 기간으로 쪼갠다(`DECISIONS-2026H2.md`).

이 파일은 2026-09-12 03:13 에 만들었다. 그 전의 결정은 `docs/HANDOFF.md` 와 git 이력에 있다.

---

26.09.12 02:15 · 국문 대본의 영문 표기를 한글 발음으로 바꾼 것을 유지한다(`Ctrl+S`→컨트롤 에스, `Ø`→파이 등 165자리) · 한국인 청자에게 한글 발음이 더 또렷하게 들린다. 길이 동일성만으로 "오독 아님"을 주장한 내 근거는 부족했다 · 실제 낭독을 들어 보고 부자연스러우면 뒤집는다
26.09.12 02:15 · 감정 태그는 원본 `SCRIPT.md` 를 건드리지 않고 별도 파일(`SCRIPT.tts.{ko,en}.md`)로 낸다 · 원본은 덱의 발표자 노트로도 실리고 문단 수·비트 번호가 화면·음성과 묶여 있다 · 태그를 읽는 엔진으로 완전히 옮겨가면 그때 재검토
26.09.12 02:15 · PR #50 은 보류한다(머지하지 않는다) · 국문 7차시 `narration-timing.json` 이 검토 반영 전 대본에 묶여 CI 가 빨갛다. 음성을 다시 만들면 같이 풀린다 · 복제 음성 작업이 무산되면 Heami 로 재합성해 푼다
26.09.12 02:40 · 음성 생성을 Colab 이 아니라 이 컴퓨터 GPU(RTX 4070 SUPER · 12GB)에서 한다 · Colab 은 내가 조작할 수 없고(구글 계정 로그인) 무료 한도가 끊긴다. 같은 스크립트가 양쪽에서 도니 노트북은 예비로 남긴다 · 로컬 생성이 실측 9시간을 넘으면 Colab 병행
26.09.12 02:40 · 감정 태그를 본문에서 떼고 `tone`/`mood` 로 옮긴다 · `generate_voice_clone` 에 `instruct` 인자가 **없다**(실제 시그니처 확인). 본문에 `[차분하게]` 가 남으면 엔진이 글자로 읽는다 · 태그를 읽는 엔진(ElevenLabs v3 등)으로 옮기면 태그본을 그대로 쓴다
26.09.12 02:40 · 말투는 문단이 아니라 토막(≈한 문장)마다 고른다 · 문단 단위로 묶으면 값 읽는 문장이 주변 설명에 묻혀 `slow` 가 3% 로 떨어진다(토막 기준 22%) · 낭독이 문장마다 튀어 들리면 문단 단위로 되돌린다
26.09.12 02:40 · 참조 녹음은 지금 있는 `base.wav` 하나로 간다(사용자 결정) · 바로 시작할 수 있고, 나중에 mood 별 녹음을 넣으면 `done.jsonl` 이 sha 로 가려 **그 mood 토막만** 다시 만든다 · 한 말투가 지루하게 들리면 careful·firm·light 3개를 10~15초씩 추가 녹음
26.09.12 02:40 · `base.wav` 의 `ref_text` 는 사용자가 Colab 에서 쓴 대사를 그대로 쓴다(사용자가 맞다고 확인) · 「갑습니다!」 등이 오타처럼 보였지만 사용자 확인이 1차 근거다 · 복제 품질이 낮으면 대사를 다시 받아 적는다
26.09.12 02:40 · 2~7차시 영상도 `--preview` 로 렌더한다(사용자 결정) · 음성-화면 동기를 눈으로 확인하는 용도. `prepare_lecture.py` 가 `purpose: validation-preview` 로 표시하므로 배포본과 섞이지 않는다 · AutoCAD 녹화 15개가 들어오면 배포본으로 다시 렌더
26.09.12 02:40 · 복제 음성 배속은 1.0 이다 · 1.38 은 규칙 기반 Heami 가 느려서 올린 값이고 사람 말투가 그대로 나오는 복제본에는 올릴 이유가 없다 · 들어 보고 느리면 `ingest_voice.py --tempo 1.38`
26.09.12 03:13 · 배치 크기를 8 에서 4 로 내리고 길이순으로 정렬해 묶는다 · 배치 8 에서 호스트 RAM 이 터져 프로세스가 죽었다(31.6GB 중 여유 10GB). 배치는 가장 긴 토막에 맞춰 패딩되므로 길이를 섞으면 낭비가 크다 · 실측 RSS 가 여유를 크게 밑돌면 다시 올린다
26.09.12 03:50 · `retime_frames.TITLE_SLACK = 0.6` — 제목 프레임 등장 마감에 주는 여유 · 0.15 로는 `hyperframes check` 가 오류를 냈다. 5.26초에 다 보이는 요소를 검사기가 5.476초에 관측한다(시간축을 띄워 재기 때문) · 검사기가 촘촘해지면 다시 줄인다
26.09.12 08:20 · `max_new_tokens` 상한을 대역 위끝(1.6배)으로 건다 · 실측으로 1,200토큰이 100자(13.6초)를 안 자르므로 초당 88토큰 아래다. 대역 밖 음성은 버릴 것이니 끝까지 만들 이유가 없다 · 상한이 좋은 음성을 자르면 TOKENS_PER_SEC 를 올린다
26.09.12 09:20 · `non_streaming_mode` 를 복제 호출에 넘기지 않는다(기본값 False) · True 로 덮었더니 VRAM 2.74→11.9GB, 호스트 RAM 12.7GB 로 불고 두 번 멈춰 32분을 날렸다. 기본값으로 돌리니 배치 4·최장 토막에서 4.29GB·1.9배속이다 · 라이브러리가 기본값을 바꾸면 다시 본다
26.09.12 09:24 · 오래 도는 작업은 `Start-Process` 로 세션 밖에 띄운다 · `nohup`·`disown` 은 Claude Code 세션이 끝날 때 같이 죽었다(439토막에서 멈춤) · 세션이 프로세스를 붙잡지 않게 되면 다시 본다
26.09.13 10:40 · 2~7차시 영상을 `_PREVIEW` 이름으로 릴리즈에 올린다(사용자 결정) · 음성·화면 동기를 미리 볼 수 있게 하되 배포본과 섞이지 않게 이름으로 가른다 · AutoCAD 녹화가 들어오면 `--preview` 를 떼고 다시 렌더해 이름도 바꾼다
26.09.13 10:45 · 자습 쪽 100KB 초과 2개(lesson-05-5 106.0KB · 05-6 103.6KB)를 막지 않는다 · 국문도 같은 두 쪽이 넘고 지난 릴리즈에 그대로 나갔다. 영문만 막을 이유가 없다 · 쪽을 더 쪼갤 수 있으면 그때 줄인다

2026-09-14 00:42:43 KST · 사용자가 대본 통합·검수용 영상과 GitHub 릴리즈를 위임했다. 최신 16개 MP4와 두 언어의 자료를 언어별 ZIP으로 배포한다. 실제 미녹화 실습과 사람 청취 상태를 명시한다.
2026-09-14 00:42:43 KST · 기존 자습 100KB 초과 허용 결정을 종료한다. 도해 포함 실제 페이지 크기로 단계 경계 분권을 수정했고 국문·영문 모두 상한 이내로 빌드했다.

# HANDOFF 보관 — 2026년 9월 (2)

최신 상태는 [HANDOFF](../HANDOFF.md)에 있다. 아래는 당시 기록을 보존한 것이며, 본문의 상대 경로는 원래 `docs/HANDOFF.md` 위치를 기준으로 읽는다.

---

## 2026-09-14 / 국문·영문 대본 통합 검수 릴리즈 공개

- 사용자 위임에 따라 [검수 릴리즈](https://github.com/Simon-YHKim/Lecture/releases/tag/autocad-2026.09.14-review.1)를 공개했다. 국문·영문 MP4 8개씩, 전체 ZIP 2개, 영상 제외 자료 ZIP과 개별 덱·워크북 등 28개 자산이다. 모든 다운로드를 비로그인 HTTP 200·크기·원격 SHA-256으로 확인했다.
- 릴리즈 소스는 `cd73dbe868cd2dc12d3aca24eed3e3d6d4d43f4f`다. 원본 승인 대본 16개, 본문 변경 68개, 현재 음성 2,223토막을 반영했으며 국문 총 211:43·영문 총 253:27이다. 배속은 1.00이다.
- 슬라이드 국문 222장·영문 263장에 차시별 전체 대본을 넣고 자습 분권 40개씩과 통합 워크북을 함께 제공했다. ZIP 전체 압축을 해제하고 `START_REVIEW_KO.html` 또는 `START_REVIEW_EN.html`을 열면 대본 클릭 이동과 시간별 의견 JSON 내보내기를 사용할 수 있다.
- MP4 16개 전체 디코딩·자막 문자/시각 왕복·장면별 음성/인코딩 화면 대조, 원본 장면 검사 170개, 자동 테스트 225개, 국영문 브라우저·공개 자료 가드·ZIP CRC를 통과했다. 화면 자동 비교의 글꼴 위치 차이는 실제 두 화면을 본 뒤 원본·MP4 해시에 묶어 기록했다.
- `<>` 측정값 자리표시는 원문을 보존한 VTT 결합으로 해결했다. 모든 2,223개 자막의 문자·시각을 확인했으며 검수 플레이어·MP4 내장 자막·VTT 사용을 안내했다.
- **최종 강의 승인판은 아니다.** 언어별 2~7차시 실제 AutoCAD 실습 녹화 15자리는 PREVIEW이며 실제 실행·전체 사람 청취는 남아 있다. 영문 4차시 14:03~14:36 F8/ortho와 16:47~17:03의 315 뒤 반복 가능 구간을 우선 검수한다. 검수자에게 메시지는 보내지 않았다.
- `local-materials/release-review-current.json`이 제작 기록 정본이다. 여기의 비공개 위치에 원본 MP4·프로젝트·검증 로그를 보존했다. `assets/release-manifest.json`·`release-upload-plan.json`·`public-release-validation.json`이 최종 파일 및 공개 검증 근거다. 참조 음성·개별 WAV·ASR 원문·개인 경로는 공개하지 않았다.

---

## 2026-09-13 / 국영문 대본 검토본 16개 · 시각 생성기 수정

- `SCRIPT_REVIEW_PROMPT.md`를 `/vibe`로 수행했다. 1~4차시와 5~8차시 편집을
  나눠 **68문단을 수정한 전체 대본 16개**를 만들었다. 원본 `SCRIPT.md`와
  `SCRIPT.en.md`에는 아직 반영하지 않았으며 음성 합성·영상 렌더·공개 게시도
  실행하지 않았다. 7차시 영문의 소수 자릿수 표현 모순도 수치를 보존해 고쳤다. 발음 개선은 새 음성을 청취한 뒤 판단해야 한다.
- 로컬 산출물은 `local-materials/script-review-20260913-172646/`에 있다.
  `review-report.html`에서 변경 이유와 전체 대본을 보고,
  `AutoCAD_Reviewed_Scripts_20260913.zip`의 `ready/`에서 원본 형식의 16개를 받는다.
  `reviewed/`는 받은 시각 토큰을 보존한 편집 작업본이라 시각 위치가 부정확하다.
  원본에 반영할 때는 `ready/`의 본문 변경만 사용한다.
- **언어별 문단은 706개(무음 8개 포함), 합성 단위는 488개**다. 녹화 단계
  97개는 여러 문단을 묶는다. 이전 문서의 ‘488문단’은 합성 단위를 뜻했다.
- `scripts/part/timed_script.py`가 합성 단위의 시각을 물리적 줄에 차례로 붙이던
  오류를 고쳤다. 일반 문단은 첫 줄에, 녹화 단계는 제목 아래에 전체 구간을
  표시한다. 오래된 대사 해시·프레임 불일치·누락된 단위를 거부하며,
  현재 음성 자료로 정본 `SCRIPT.timed.ko.md`/`SCRIPT.timed.en.md` 16개를 다시 만들었다.
  이 정본 시각본은 **기존 원본 대본**의 시각이며 편집본의 새 낭독 시각이 아니다.
- 기존 낭독 합계는 국문 **209:21**, 영문 **250:31**(+19.7%)이다.
  국문 영상 **211:25**에는 프레임 유지 시간이 더해지므로 대본 시각과 시간축이 다르다.
  단계 안 개별 문단의 실측 시간은 제공되지 않았다.
- 검증: 부품 시험 **160개**(새 회귀 시험 10개 포함), 비공개 자료 검사,
  과정 규격·판 대조 검사 통과. 본문·비트·단계·입력값과 지정 보호 표기를 대조했고,
  원본 대본 16개와 세션 시작 때 있던 보고서 2개의 해시가 유지됐다.
  편집본 적용 시 기존 청킹 규칙상 새로 만들거나 바뀌는 음성 토막은 **111개**다.
- 후속 확인: 2차시의 접점 스냅 ‘필요할 때 선택’과 3·4차시의 상시 선택 설명,
  긴 일반 문단의 비트 분할, 보호 규격 표기의 발음을 `decisions.html`에 남긴다.
  문단 구조·기술 절차를 바꾸거나 음성을 듣지 않고 해결됐다고 처리하지 않는다.
- 다음 순서: 편집본 검토 → 원본 본문 반영 → jobs-ko/en → 바뀐 토막 합성 →
  ingest_voice → retime_frames → prepare_lecture → 검증·렌더.
  영문 시각본의 Voice·Time을 원본에 통째로 덮어쓰지 않는다.
  2~7차시는 실습 녹화가 없으면 기존 preview 제한을 유지한다.

## 2026-09-12 (6차) / 복제 음성 — 이 컴퓨터 GPU 로 국문·영문 낭독

### 어디까지 왔나

- **음성을 이 컴퓨터에서 만든다.** RTX 4070 SUPER · 12GB · 여유 9.6GB ·
  compute 8.9 (bf16 됨) · torch 2.11.0+cu128 · qwen-tts 0.1.1. Colab 은 예비로
  남겨 두었다(`docs/autocad-technician/qwen-narration.ipynb` — 같은 스크립트가
  양쪽에서 돈다).
- 새 파일: `scripts/part/tts_jobs.py` · `speak_clone.py` · `ingest_voice.py` ·
  시험 둘. `narrate_tts.py` 에 복제 음성 한 줄.
- 부품 시험 141개 통과. 규격·판 대조·비공개 자료 관문 통과.

### 목소리와 감정은 **동시에 못 가진다** — 실제 시그니처로 확인

```
generate_voice_clone (text, language, ref_audio, ref_text, x_vector_only_mode,
                      voice_clone_prompt, non_streaming_mode, **kwargs)   ← instruct 없음
generate_custom_voice (text, speaker, language, instruct, ...)            ← 목소리 고정 9명
generate_voice_design (text, instruct, language, ...)                     ← 목소리 고정
```

`SCRIPT.tts.ko.md` 의 `[차분하게]` 를 Base 에 그냥 넣으면 **글자로 읽힌다.**
태그를 본문에서 떼고 `tone`/`mood` 로 옮겼다. 이 모델은 `ref_text` 를 함께
주면 ICL 로 **참조 음성의 말투까지** 따라가므로, mood 마다 다른 짧은 녹음을
두면 그 말투로 읽는다. 태그본은 태그를 읽는 엔진으로 옮길 때 쓰게 남겼다.

**말투는 문단이 아니라 토막(대충 한 문장)마다 고른다.** 문단 단위로 묶으면
값을 읽는 문장이 주변 설명에 묻혀 전부 `calm` 이 된다(문단 기준 3%, 토막 기준
22%).

| mood | 참조 음성이 맡는 몫 | 국문 토막 | 영문 토막 |
| --- | --- | --- | --- |
| base | calm · point | 762 (64%) | 495 (48%) |
| careful | slow | 262 (22%) | 229 (22%) |
| firm | warn · stress | 85 (7%) | 235 (23%) |
| light | ask · light · warm | 83 (7%) | 77 (7%) |

**사용자 결정: 지금은 `base.wav` 하나로 간다.** 네 mood 가 모두 그 하나를
쓰므로 억양은 한 가지로 나온다. 나중에 `refs/careful.wav` 등을 넣으면 **그
mood 의 토막만** 다시 만들면 된다(전체 재생성 아님 — `done.jsonl` 이 sha 로
가린다). `ref_text` 는 사용자가 Colab 에서 쓴 대사가 맞다고 확인해 그대로 쓴다.

감정을 낼 수단이 참조 음성 하나뿐이라, **문장 사이 호흡**을 말투마다 다르게
줬다(`ingest_voice.CHUNK_GAPS`) — 값을 읽는 문장 앞은 0.30초, 툭 던지는 말
앞은 0.14초. 문단 사이 0.45초는 화면 동기의 규격값이라 건드리지 않는다.

### 문단이 너무 길다 — 규칙 기반 음성에는 없던 문제

합성 단위인 문단은 최대 **1,493자**다. SAPI 는 길이를 타지 않지만 자기회귀
모델은 긴 입력에서 문장을 삼키거나 끝을 잘라먹고, **잘려도 소리는 정상으로
난다.** 선생님 목소리로 문장 반쪽이 나가는 사고다.

- 문장 경계에서 토막을 낸다. 국문 목표 100자/상한 150, 영문 260/380.
- 488문단 → **국문 1,184토막**(중간 82자), **영문 1,036토막**(중간 208자).
- 토막을 공백 하나로 이으면 원문이 **글자까지** 복원된다. `speakable()` 이
  모든 공백을 하나로 줄여 두기 때문이다. `test_tts_jobs.py` 가 그것을
  `synthesis_plan` 과 직접 대조한다.
- **쪼개는 함수는 한 곳에만 있다** (`tts_jobs.lesson_rows`). 작업 목록과
  받아쓰기가 따로 쪼개면 토막 번호가 어긋나 엉뚱한 소리가 붙는다.
- 굵게 표시(`**…**`)도 걷어낸다. 국문 7문단·영문 1문단. `speakable()` 은
  백틱만 걷으므로 별표가 엔진까지 따라온다.
- `speak_clone.py` 는 기대 길이(국문 초당 5.5자)의 0.45~2.2배 밖이면 낱개로
  세 번까지 다시 만들고, 그래도 안 되면 가장 가까운 것을 남기고
  `suspect.jsonl` 에 원문과 함께 적는다 — **조용히 버리지 않는다.**

### 속도 — 배치가 유일한 수단

4070 에서 낱개로 돌리니 **토막당 27초 · 실시간 0.28배**였다(국문만 9시간).
flash-attn 은 Windows 빌드가 필요해 안 쓴다. 그래서 `speak_clone.speak()` 가
같은 참조 음성을 쓰는 토막을 묶어 한 번에 넣고(`--batch 8`,
`non_streaming_mode=True`), 참조 음성은 `create_voice_clone_prompt` 로 한 번만
부호화한다.

### 배속은 1.0

1.38 은 Heami 가 느려서 올린 값이고 복제 음성에는 올릴 이유가 없다.
`narrate_tts.CLONES` 에 `Simon Clone (Qwen3-TTS): 1.0` 을 넣고
`verify_tempo_timing` 이 복제 음성에는 `tempoMethod: voice-clone-native` 를
요구하게 했다. Heami·Zira 쪽 검사는 한 줄도 느슨해지지 않았다. 같은 속도로
맞추고 싶으면 `ingest_voice.py --tempo 1.38` 이 기존 ffmpeg 두 갈래를 쓴다.

### 영상 — 1·8차시만 배포본이 된다

2~7차시 프레임에는 AutoCAD 화면 녹화가 들어갈 자리가 비어 있다
(`DEMO-01`, `DEMO-01A/B/C` ×5 — **총 15개**). `prepare_lecture.py` 는 녹화가
없으면 `purpose: validation-preview` 로만 내보내고 배포본을 거부한다.
**사용자 결정: 2~7차시도 preview 로 렌더한다** — 음성·화면 동기를 눈으로
확인하는 용도이고 배포본이 아니다. 1·8차시는 녹화 자리가 없어 그대로 완성본이다.

### 쓰는 순서

```
python scripts/part/tts_jobs.py local-materials/qwen-narration --lang ko
python scripts/part/speak_clone.py --work local-materials/qwen-narration --lang ko
python scripts/part/ingest_voice.py --in local-materials/qwen-narration/out/ko \
                                    --out <저장소 밖 폴더>
python scripts/part/retime_frames.py <차시>      # 실측 시각으로 프레임 다시 맞춤
python scripts/part/prepare_lecture.py <차시> --out … [--preview]
```

### 막힌 것

- **PR #50 은 여전히 빨갛다.** 국문 7차시의 `narration-timing.json` 이 검토
  반영 전 대본에 묶여 있다. 복제 음성으로 다시 만들면 같이 풀린다.
- 2~7차시 배포본 영상은 사용자의 AutoCAD 녹화 15개를 기다린다.

---

## 2026-09-11 (5차) / 영문 대본 여덟 차시 완성 · 프레임 넘침 0 · 공개판 preview.2 (PR #39~#43)

### 어디까지 왔나

- main HEAD **`1c598cf`** (PR #45) · CI 초록 · 열린 PR 0 · 워킹 트리 깨끗.
- 공개판 **`autocad-2026.09.11-preview.2`** 게시 — 자습/강의 × 국문/영문 12개 파일.
- **영문 대본이 여덟 차시 다 갖춰졌다.** 2~7차시 627문단을 이 세션에서 썼다.
- 영문 음성도 2~7차시를 합성했다(Zira 1.15배). 프레임 시각까지 맞췄다.
- **영문 프레임이 캔버스 밖으로 넘치던 것을 찾아 고쳤다.** 78장을 전수로 재
  보니 영문 11장이 잘리고 있었다. 지금은 0장이다.

| 무엇 | 상태 |
| --- | --- |
| 영문 자습 교재 | 완료 |
| 영문 도면 | 완료 |
| 영문 프레임 지도 | 여덟 차시 973줄 |
| 영문 대본 | **여덟 차시 706문단 완료** |
| 영문 음성 | **여덟 차시 완료** (2~7차시는 이 세션) |
| 영문 영상 | 1·8차시만 (2~7차시는 녹화 대기) |

### 영문 대본은 국문과 **같은 뼈대**여야 한다

문단이 하나만 달라도 그 차시의 강조 시점이 통째로 밀린다. 그래서 번역이기
이전에 구조가 같아야 한다 — 같은 Line, 같은 문단 수, 같은 비트 번호.

- `test_script_editions.py` 가 Line·문단·비트를 검사한다. 영문 대본이 있는
  차시만 본다.
- 비트는 문단 첫 글자가 `(1` 처럼 **괄호와 숫자**로 시작할 때만 잡힌다.
  「(card 1 · …)」로 쓰면 비트가 통째로 사라진다. 「(1 card — …)」로 쓴다.
- 녹화 Line 의 단계 제목은 국문 `### 3단계`, 영문 `### Step 3` 둘 다 읽는다
  (`beats._STEP_HEAD`).
- **단계별 명령 표기(`step_keys`)와 첫 사용 순서(`shortcuts_in`)가 국문과
  완전히 같은지도 맞춰 두었다.** 옵션으로 치는 글자가 명령으로 세어지지 않게
  영문에도 「chamfer option」「fillet option」「the layer option」「the close
  option」「current layer」를 적었다. 안 적으면 그 차시의 명령표가 어긋난다.

| 차시 | 문단 | 단계 | 명령 |
| --- | --- | --- | --- |
| 2 | 130 | 16 | — |
| 3 | 85 | 16 | 15 |
| 4 | 100 | 16 | 24 |
| 5 | 105 | 16 | 11 |
| 6 | 104 | 16 | 17 |
| 7 | 103 | 17 | 17 |

### 프레임 넘침 — 원인은 번역 길이가 아니었다

`.clip` 은 `overflow:hidden` 이라 넘친 부분이 **조용히 잘린다**. 잘린 화면은
"원래 그런 그림" 처럼 보이므로 눈으로는 못 찾는다. 한계선은 **1016px** —
1080 캔버스에서 `.clip` 아래 여백 64 를 뺀 값.

원인은 표 설명 칸의 인라인 `white-space:nowrap` 이었다. 국문은 「선을 긋는다」
처럼 짧아 문제가 없지만, 영문은 같은 자리가 한 문장이라 그 칸이 624px 를 먹고
옆 칸이 114px 로 찌그러진다. 그러면 옆 칸이 네댓 줄로 접히며 표가 통째로
내려간다. 3차시 명령표가 724px 자리에서 **1470px** 이 됐고 4차시에서는 겹침
오류까지 났다.

- `build_english_lesson.py` 가 사본에서 **먹색 설명 칸의 줄바꿈 금지만** 푼다.
  명령 이름·치수 값이 들어가는 강조색 칸은 그대로 둔다.
- 그래도 남은 것은 문장을 줄였다 — 카드 6개, 명령 설명 38개.
  **명령 설명은 여덟 차시가 같은 문장을 쓰므로 지도 전체에 한 번에 건다.**
  한 차시만 고치면 같은 명령이 차시마다 다르게 설명된다.
- **문장을 깎는 것으로 닫히지 않는 자리가 있다.** 표가 자동 배치라 한 칸을
  줄이면 열 너비가 다시 나뉘면서 다른 행이 대신 두 줄이 된다 — 6차시 명령표는
  세 줄짜리 두 행을 없앴는데 전체 높이가 585px 그대로였다. 그래서 영문 사본에서만
  `table.spec.tight` 의 셀 여백을 6px→3px 로 좁힌다. 글자 크기는 안 건드린다.

```
국문 78장 넘침 0 · 영문 78장 넘침 11 → 0
```

### 재는 방법 (`scripts/part/frame_sheet.py`)

프레임 본문은 `<template>` 안에 있어 파일을 그냥 열면 아무것도 안 보인다.
측정 쪽이 `fetch` 로 읽어 복제해 붙인다.

```bash
python scripts/part/frame_sheet.py <잴 폴더> --en <영문 사본들이 모인 폴더>
cd <잴 폴더> && python -m http.server 8731 --bind 127.0.0.1
# 브라우저(Playwright MCP headless)로 measure.html 을 열고 await measureAll()
```

⚠ **이 측정값은 상대 비교용이다.** hyperframes check 는 JetBrains Mono 를
받아 `@font-face` 를 주입하므로 같은 프레임이 30~60px 더 높게 잡힌다.
넘침의 최종 판정은 언제나 `hyperframes check` 다.

### 영문 강의를 만드는 길 (4차 블록과 같다, 한 줄 추가)

```bash
python scripts/part/build_english_lesson.py <국문 차시> <비공개 사본>
python scripts/part/narrate_tts.py <사본> --voice "Microsoft Zira Desktop" --out <새 폴더>
python scripts/part/retime_frames.py <사본>
python scripts/part/prepare_lecture.py <사본> --out <렌더 폴더> --gsap <gsap.min.js> --preview
python scripts/part/prepare_scene_checks.py <렌더 폴더> --out <장면 폴더>   # ← 긴 차시는 이것부터
sh scripts/part/run_scene_checks.sh <장면 폴더> <이름표>
npx hyperframes@0.8.33 check <렌더 폴더> --json --at-transitions
```

**긴 차시는 통짜 check 의 모션 판정을 믿으면 안 된다.** 0.8.33 은 모션을 300점
까지만 훑으므로 30분 차시는 6초에 한 번 본다. 그러면 2초짜리 등장이 통째로
"늦게 나타남" 오류가 된다. `prepare_scene_checks.py` 가 장면별로 쪼개 준다.
**레이아웃·대비 판정은 통짜 check 도 정확하다.**

여섯 차시 71장면을 전부 돌렸다 — **오류 0**. 그 과정에서 **4차시 명령표의 행
조건이 옛 값으로 남아 있던 것**을 찾았다(PR #41). 24개짜리 표는 두 쪽으로
넘어가는데, 시각을 다시 맞추면 넘어가는 시점이 83.57→92.82초로 가지만 행 조건은
`retime_frames.sync_motion` 이 못 찾아 국문 값으로 남았다. 행의 등장이 스물넉
줄을 **한 선택자에 묶어** 부르기 때문이다. 지금은 그 행을 켜는 `tl.to` 를 본다.

⚠ **검사는 메모리를 많이 먹는다.** 71장면을 한 번에 돌리다 두 번 끊겼다.
`npx` 로 띄운 wrapper 가 끝나도 남아 크롬 자식까지 붙들고 있다(32개 · 약 0.8GB).
`scripts/part/run_scene_checks.sh` 는 이미 통과한 장면을 건너뛰므로 끊긴 자리에서
이어 돌린다.

### 이 세션의 영문 음성 길이 (Zira 1.15배, 실측)

| 차시 | 길이 | | 차시 | 길이 |
| --- | --- | --- | --- | --- |
| 2 | 30:06 | | 5 | 33:55 |
| 3 | 30:48 | | 6 | 31:54 |
| 4 | 31:08 | | 7 | 36:27 |

국문보다 3~5분 길다. 1.15배가 1.38배보다 느린 것이 그대로 반영된 값이다.

### 사용자가 답한 것 (2026-09-11)

- **국문 낭독은 1.38 유지.** 영문만 1.15 다. 더 묻지 않는다.
- 저장소 루트의 `--help` 는 **지우지 않는다.**
- 머지 끝난 원격 브랜치 **정리 완료** — 21개 삭제. 남은 것은 `main` 과
  `handoff/20260911-0022` 뿐이다. 그 하나는 **머지되지 않은 브랜치**라
  자동 정리 대상이 아니어서 남겼다(§7). 내용은 1차 블록이라 이미 보관으로 갔다.
- 녹화는 나중에.

### 공개판 — `autocad-2026.09.11-preview.2`

버킷 넷으로 나눠 올렸다. 파일 이름에 `KO` / `EN` 이 박혀 있다.

| 버킷 | 파일 |
| --- | --- |
| 자습 · 국문 | `SelfStudy_KO.zip`(43장) · `Workbook_KO.html` |
| 자습 · 영문 | `SelfStudy_EN.zip`(37장) · `Workbook_EN.html` |
| 강의 · 국문 | `Scripts_KO.zip`(8차시) · `Deck_KO.html`(222장) |
| 강의 · 영문 | `Scripts_EN.zip`(8차시) · `Deck_EN.html`(263장) · `Lecture_EN_L01/L08.mp4` |

`SHA256SUMS.txt` 와 `release-manifest.json` 을 함께 올린다. 매니페스트에는
파일마다 버킷·크기·해시가 들어 있다. 미디어는 Git 이력 밖에 두고 완성 파일만
Release 첨부로 공개한다는 규칙은 그대로다.

⚠ **커밋된 자습 페이지가 원본보다 낡아 있었다**(PR #43). 머리글이
`작성 x · 자습 약 28분 (영상 07:20)` 로 PR #16 시절 값이었다. 릴리즈를 만들다
발견했다. 자습 원본을 고치면 `build_selfstudy.py` 를 다시 돌려 산출물까지
커밋해야 한다 — 안 그러면 사이트와 릴리즈가 서로 다른 판을 말한다.

### 남은 것

| # | 작업 | 크기 | 메모 |
| --- | --- | --- | --- |
| A | 2~7차시 실제 AutoCAD 2024 녹화 | large | **사용자 몫.** 국문·영문 영상이 같이 막혀 있다. 대본·슬라이드·프레임은 다 서 있다 |
| B | 1·8차시 **국문** 영상 다시 렌더 | medium | 개편 뒤 렌더한 적이 없다. 공개된 것은 에피소드로 쪼갠 옛 판이다. 국문 음성(Heami 1.38)을 사본에서 다시 합성하면 영문과 같은 길로 간다 |
| C | 작도 안내 P2 잔여 5건 | medium | 실제 조작·원본 대조 필요 → A 와 함께 |

### 영문 덱 — 막혔던 자리 셋 (PR #45 로 닫음)

```bash
SELFSTUDY_LANG=en python scripts/selfstudy/build_deck_all.py <출력> <작업폴더> \
    --standalone <gsap.min.js> --root <영문 사본 묶음> --lang en
```

1. **절을 찾는 열쇠가 한국어 문자열이었다.** 덱은 자습본의 절을 `EXTRA` 에 적힌
   국문 제목으로 찾아 프레임 뒤에 끼운다. 표시용 `ko()` 가 언어를 따르게 하자
   그 열쇠까지 영문이 되어 「자습본에 「점을 찍는 네 가지 방법」 절이 없다」로
   멎었다. → 찾는 열쇠는 `ident()` 로 갈라 **언제나 국문**.
2. **덱이 제 손으로 쓰는 이름표.** 조작 종류·카드 머리·스냅 이름·형상 이름이
   파이썬 문자열에 박혀 있었다. → `_UI_KO`/`_UI_EN` 처럼 표를 둘로.
3. **아래 막대.** → `deck_nav.en.html`. 검수 메모 패널은 영문판에 달지 않는다
   (그 패널의 쓰임이 「검수 후 메모 파일을 대화창에 첨부하세요」다).

보이는 한글 **43,991자 → 446자**. 국문 덱은 재빌드 SHA-256 이 같다.
`test_deck_language.py` 가 표끼리 짝·서식 자리·막대의 한글 0 을 지킨다.

**남은 446자는 내용 쪽 결정이 필요하다.**

| 남은 것 | 왜 |
| --- | --- |
| 레이어 이름 다섯 (`외형선`·`중심선`·`숨은선`·`치수선`) | 국문 과정에서 **실제로 타이핑하는 값**이라 자습 원본에 `{ko,en}` 이 아닌 평문으로 있다. 영문 대본·영문 도면은 `Visible line`·`Centerline` 을 쓰므로 맞추려면 원본을 고쳐야 한다 |
| 강의 프레임 안 도면의 뷰 이름 (`정면도`·`평면도`·`우측면도`) | `build_english_lesson.py` 가 **SVG 를 번역 대상에서 뺀다**(주석에 명시). 프레임의 도면을 `edu_ib_02.py --lang en` 으로 다시 뽑아 갈아 끼우면 되지만, 이미 검증·렌더까지 끝난 프레임이 바뀐다 |

### 다음 세션 시작하는 법

```bash
git fetch origin main && git pull --ff-only origin main
cat docs/HANDOFF.md
python -m unittest discover -s scripts/part -p 'test_*.py'   # 126개 통과여야 한다
python scripts/selfstudy/check_english.py                    # 통과여야 한다
```

---

## 2026-09-11 (4차) / 영문 강의가 굴러간다 — 1·8차시 영상 완성 · 프레임 지도 여덟 차시 (PR #34~#37)

### 어디까지 왔나

- main HEAD **`866f426`** · CI 초록 · 열린 PR 0 · 워킹 트리 깨끗.
- **영문 1·8차시가 영상까지 나왔다.** 녹화가 필요 없는 두 차시다.
  국문도 이 둘만 영상이 있으므로, 두 판의 진도가 같아졌다.
- 사용자가 영문 낭독 속도를 **1.15배**로 정했다(1.38배 표본을 듣고 빠르다고 판단).
  국문 1.38 은 그대로다. **국문도 내릴지는 사용자 확인 대기.**

| 무엇 | 상태 |
| --- | --- |
| 영문 자습 교재 | **완료** (도해 영문 짝 161자리 · 넘침 0) |
| 영문 도면 | **완료** (`--lang en` · 다섯 글자만 영문) |
| 영문 프레임 지도 | **여덟 차시 973개 전부** |
| 영문 대본 | **1·8차시 완료** · 2~7차시 남음 |
| 영문 영상 | **1·8차시 완료** (6:04 · 6:50) |

### 영문 강의를 만드는 길

```bash
python scripts/part/build_english_lesson.py <국문 차시> <비공개 사본>
python scripts/part/narrate_tts.py <사본> --voice "Microsoft Zira Desktop" --out <새 폴더>
python scripts/part/retime_frames.py <사본>
python scripts/part/prepare_lecture.py <사본> --out <렌더 폴더> --gsap <gsap.min.js>
npx hyperframes@0.8.33 check <렌더 폴더> --json --at-transitions
npx hyperframes@0.8.33 render <렌더 폴더> --quality high --fps 30 --low-memory-mode --output <mp4>
```

**국문 프레임은 건드리지 않는다.** 사본에서만 갈아 끼운다. 필요한 것 둘 —
`SCRIPT.en.md`(문단·비트가 국문과 같아야 한다)와 `scripts/part/frames_en/<차시>.json`.

- 배속은 목소리를 따른다 — `TEMPOS = {Heami: 1.38, Zira: 1.15}`. 시각 파일에 적힌
  목소리로 판정하므로 국문 시각은 그대로 통과한다.
- 번역 단위는 **한 문장을 담은 가장 바깥 요소**다. `<b>` 강조가 섞여도 한 단위다.
  자리는 글자 찾기가 아니라 **파싱한 오프셋**으로 덮는다.

### 남은 것 — 영문 대본 2~7차시

문단 수가 국문과 한 개라도 다르면 그 차시의 강조 시점이 통째로 밀린다.
`test_script_editions.py` 가 Line·문단·비트 번호를 검사한다.

| 차시 | 문단 | 비고 |
| --- | --- | --- |
| 2 | 130 | Line 8 이 16단계 42문단 |
| 3 | 85 | Line 5 가 40문단 |
| 4 | 100 | Line 5 가 47문단 |
| 5 | 105 | Line 5 가 64문단 |
| 6 | 104 | Line 5 가 58문단 |
| 7 | 103 | Line 5 가 56문단 |

합계 **627문단**. 국문 대본 한글 61,872자에 대응한다.

### 사용자 확인 대기

- **국문 낭독도 1.15 로 내릴까.** 지금은 영문만 1.15 다. 국문까지 내리면 8차시
  음성을 다시 합성하고 프레임 시각을 다시 맞춰야 하며, 공개된 첫 판과 달라진다.
- 저장소 루트의 추적 파일 **`--help`** (31,150B · 참조 0) 삭제 여부.
- 머지 끝난 원격 브랜치 정리 여부.

### 다음 세션 시작하는 법

```bash
git fetch origin main && git pull --ff-only origin main
cat docs/HANDOFF.md
python scripts/selfstudy/check_english.py            # 통과여야 한다
python scripts/part/frame_text.py projects/autocad-technician/lesson-02-part-and-template --map scripts/part/frames_en/lesson-02-part-and-template.json
# 2차시 SCRIPT.en.md 부터. 1·8차시를 본보기로 삼는다.
```

---

---

## 2026-09-13 / 승인 본문 반영 · 추가 음성 교차 검수와 시간 갱신

- **전체 국영문 강의 패키지의 최종 납품은 미완료다.** 이번 완료 범위는 승인된
  대본 수정과 음성·시간 자료 반영이다. 슬라이드는 기존 국영문 덱과 HTML 원본이
  있으나 이번 개정 기준의 최종 덱 묶음을 다시 만들지 않았다. 영상은 2~7차시
  실습 녹화와 최신 음성을 반영한 국영문 전체 MP4 제작·최종 검수가 남아 있다.
  자습본도 기존 국영문 8차시가 있으나 이번에 전체 최종본으로 승인한 것은 아니다.
  2026-09-13 후속 상태 확인에서 `SELFSTUDY_SRC=scripts/selfstudy/source`를 지정해
  `validate_content.py`를 실행한 결과 **6/8 통과**: 4차시 138,206 B와 5차시
  130,810 B가 원본 JSON 한도 122,880 B를 초과했다. 입력값·단축키 대조 경고도
  남는다. 이는 원본 JSON 검사 결과이며 출력 HTML의 용량 초과를 뜻하지 않는다.
  대상이 잡히지 않은 기본 실행의 `0/0 통과`는 검증 근거로 사용하지 않는다.

- 작업 브랜치: `feat/clone-voice-pipeline`. 사용자가 승인한 **1~4차시 38개 +
  5~8차시 30개 = 68개 본문 변경**은 원본에 반영된 상태다. 원본 16개 모두
  `ready/`의 본문과 일치하며 본문 변경 파일은 13개다. 후속 음성 검수에서는
  본문·입력 리터럴·수치 순서·보호 표기·비트 구조·화면 글자를 추가 변경하지 않았다.
- Git 제외 `local-materials/script-apply-current.json`이 현재 작업 위치의 정본이다.
  `base/followup-report.html`이 최신 보고서이며 `followup-applied-scripts-and-timing.zip`은
  원본 16개·시각본 16개·TTS 태그본 16개·언어별 시간 JSON 16개를 담는다.
  이전 `completion-report.html`과 ZIP은 최초 반영 시점의 기록으로 보존했다.
- 최초 변경 음성 **국문 30개·영문 81개 = 111개** 전체를 로컬 Whisper large-v3와
  Qwen3-ASR-1.7B로 교차 대조했다. 두 모델에 정답 대본을 제공하지 않았다.
  26개 토막에서 재합성 후보를 비교해 **국문 9개·영문 16개, 합계 25개를 추가 교체**했다.
  개선 근거가 없는 1개는 기존 음성을 유지했다. 재사용 **2,112개는 바이트까지 동일**하고
  이전 음성·발음 입력·Git diff·프로젝트 사본·검토본을 보존했다.
- 기존 0.6B 모델을 기본으로 유지하고, 7개 영어 후보에는 기존 로컬 캐시의
  **Qwen3-TTS-12Hz-1.7B-Base**를 사용해 모두 채택했다. 참조 음성은 동일하며 배속은
  **1.00**이다. 실제 청취를 통한 음색·억양의 일관성 평가는 수행하지 못했다.
  발음 입력과 원본을 분리하고 `spokenSha`·모델 revision·현재 WAV 해시를 기록했다.
  `pronunciation-ledger.json`은 현재 낭독 입력으로 갱신했다.
- **직접 청취는 미완료다. 이 세션은 오디오 입력을 지원하지 않는다.**
  현재 WAV 111개와 두 ASR 결과의 해시·샘플레이트·무음·클리핑 검사를 통과했다.
  73개는 최초 교차 대조에서 정규화 일치, 12개는 경미한 표기 차이를 검토했고,
  나머지 26개는 후보별 근거를 남겼다. **우선 청취 항목은 2개**이며 오독 확정 수는 아니다.
  영어 4차시 `L05-0-03#01`의 ortho 발음, `L05-0-05#04`의 315 뒤 반복 여부다.
  전체 111개의 직접 청취 판정은 비워 두었다. 최신 청취표는 locator의 `listening_page`와
  `private/audio-qa/review.html`이다. 실제 청취 후 `listening-feedback.json`을 저장할 수 있다.
- 선택한 음성을 연결하고 바뀐 10개 차시 사본의 시간 자료를 재생성했다. 16개 전체의
  현재 낭독 합계는 국문 **209:38**, 영문 **251:22**이며,
  프레임 유지 시간을 더한 영상 합계는 국문 **211:43**, 영문 **253:27**이다.
  국문 시간·프레임은 정본에, 영문 시간·프레임은 `private/lessons-en`에 반영했다.
  정본 국영문 시각본과 TTS 태그본도 갱신했으며 언어별 시간축을 분리했다.
- 이번에 변한 장면 **24개 검사 모두 통과**(CLI 0.8.33). 이전의 53개 검사와 구분한다.
  국문 8차시와 변경된 영문 7차시 과정 검사, 규격·강의/자습 대응·비공개 자료·diff 검사 통과.
  최초 반영 때 자동 테스트 **215개**가 통과했고, 이번 추가 검수에서 파이프라인 코드는
  변경하지 않았다. 새로운 시간 자료와 음성·본문 보존 검사는 다시 수행했다.
- 영문 1차시 `05-recap` 첫 비트 1.44초 경고는 기존 도입 문장의 실측 길이다.
  카드가 사라지는 시간이 아니다. 다음 카드 도입까지 25.95초 동안 뒤 설명을 담고 있으므로
  실측값·비트 구조·검사 기준을 바꾸지 않고 근거를 `existing-beat-evidence.json`에 남겼다.
- 후속 상세 기록은 `base/followup-state.json`이 가리키는 비공개 폴더에 있다.
  `selection.json`·`final-review.json`·`accepted-audio.json`·`promotion-manifest.json`·
  `verification-results.json`과 후보·이전 버전 백업을 보존한다. 개인 음성·ASR 원문은
  저장소 밖에 있으며 임시 폴더를 정리하면 연결이 끊어진다. 공개 산출물에 넣지 않는다.
- 첫 반영 때 수정한 `speak_clone.py`의 생성 상한은 코덱의 실제 12.5프레임/초를 따른다.
  관련 회귀 시험과 `--pronunciations`의 해시 검증은 유지한다. `timed_script.py`의 원본
  문단/녹화 단계별 시간 매핑 수정도 유지한다. 후속 재합성은 현재 캐시·발음 입력으로
  필요한 토막만 만들고 ASR·연결 음성·시각·장면 검사를 다시 수행한다.
- 남은 사람의 확인은 **111개 직접 청취와 억양 평가**이며 위 2개를 우선한다.
  2~7차시 실습 녹화와 접점 스냅 등 기술 절차는 기존 결정 문서에 따라 근거를 먼저
  확인한다. 이번에는 공개 게시·commit·push·MP4 렌더를 수행하지 않았다.

---

## 2026-09-14 / 대본 통합 검수 영상·릴리즈 제작 중

- 사용자가 국문·영문 강의 자료 전체와 검수용 MP4 제작, GitHub Release 게시를 명시적으로 위임했다. 이 범위의 commit·push·release에 재승인을 요청하지 않는다. 브랜치는 `feat/clone-voice-pipeline`이며 이전 변경을 보존한다.
- 현재 작업 정본은 Git 제외 `local-materials/release-review-current.json`이다. 여기의 `base`에 계획·제작 도구, `private`에 렌더 프로젝트·검사 로그·출력 파일을 둔다. 이전 `script-apply-current.json`은 승인 대본·음성 작업 기록으로 보존한다.
- **영상 제작 진행 중이며 GitHub에는 비공개 초안만 있다.** 원본 장면 검사 170개가 모두 통과했다. 현재 국문 1·7·8차시와 영문 1~5차시 총 8개 MP4가 자막 내용·시각 왕복, 전체 디코딩, 장면별 음성·화면 대조를 통과했다. 국문 7차시의 측정값 자리표시 자막도 바로잡고 음성·화면 검증을 마쳤다. 영문 도면 8개 프레임의 16개 주석은 현행 영문 표기로 변환했으며 도형·대본·음성·시각은 동일하다.
- 영상의 도면 글자 누락이라는 초기 판독은 원본 MP4 재추출로 철회했다. 해당 글자는 실제로 존재한다. 별도 두 장면의 화면 차이는 명령 글꼴 대체로 인한 위치 차이였으며, 전체 화면을 비교하고 영상·원본 해시에 묶어 근거를 기록했다. 확인되지 않은 누락을 실제 버그로 보고하지 않는다.
- 16개 최신 대본과 실제 합성 PCM 2,223개를 바이트 단위로 대조해 자막 시각을 만들었다. 검수 페이지는 대본 클릭 이동, 자막, 시간별 의견 저장·JSON 내보내기를 지원한다. 국문·영문 브라우저 검사를 통과했다.
- 슬라이드는 국문 222장·영문 263장이며 차시별 전체 대본 8개씩을 추가로 내장했다. 두 언어의 자습 분권 40파일씩과 통합 워크북을 재생성했다. 모든 분권 파일은 100KB 이하다. 영어 첫 열기를 한국어로 바꾸던 런타임 오류도 수정했다.
- 자습 입력 검사는 이제 기본으로 저장소의 원본 8개를 찾으며 빈 입력을 성공으로 보고하지 않는다. 도해와 문서 틀까지 계산해 단계 경계로 분권한다. 120KB를 넘는 통합 JSON은 실제 분권 HTML의 100KB 상한을 검사한다. 내용·명령·입력값은 바꾸지 않았다.
- 렌더는 HyperFrames 0.8.36, 1080p/30fps, high 품질이다. 남은 렌더는 실험 캡처를 끄고 beginFrame 워커 4개로 프레임을 즉시 인코딩한다. 임시 디스크 부족을 피하며, 기존 완성 파일도 장면별 실제 인코딩 화면 대조 후 유지한다. 긴 통합 영상의 300개 표본으로 생긴 모션 오판은 기존 `prepare_scene_checks.py`로 모든 원래 단언을 보존한 170개 장면 검사로 확인한다.
- 독립 검토 후 자습 첫 열기의 암묵적 언어 저장, 안내 배너를 닫았을 때 전체 대본 접근, WebVTT 특수문자 표시를 수정했다. 양 언어 브라우저 검사와 전체 자막 2,223개의 글자 보존 검사를 통과했다. 최종 묶음 생성과 업로드는 MP4 16개·자막 내용/시각·전체 디코딩·장면별 음성/실제 화면 대조·170개 원본 장면 검사 완료를 요구한다.
- 자동 테스트 225개(root 7, part 167, selfstudy 41, Node 10), 과정 검사와 공개 자료 가드를 통과했다. 발음 보정 삭제 시 캐시된 보정 음성을 재사용하던 경우도 재현 후 수정했다. 현재 제작 음성은 그대로 보존했다.
- 국문 7차시 자막 왕복 검사에서 FFmpeg SRT 처리기가 AutoCAD 측정값 자리표시 `<>`를 제거하는 것을 발견했다. 원문을 바꾸지 않고 이스케이프된 VTT로 MP4 자막을 넣어 해결했다. 실제 16차시 2,223개 자막 모두 VTT→MP4→SRT 문자·시각 대조를 통과했다. MP4 내장 자막·VTT·검수 플레이어를 권장하고, 외부 SRT 재생기의 기호 해석 차이는 안내에 적었다.
- 공개 산출물에는 원본 참조 음성·분리 WAV·ASR 원문·개인 경로를 넣지 않는다. 2~7차시의 미녹화 실습 15자리는 PREVIEW로 명시한다. 전체 사람 청취와 실제 AutoCAD 검증은 완료되지 않았으며, 영어 4차시 ortho/315 두 구간을 우선 청취 대상으로 남긴다.

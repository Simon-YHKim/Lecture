# -*- coding: utf-8 -*-
"""대본에서도 상대좌표를 걷어낸다.

남은 열여섯 자리는 전부 화면 녹화 프레임 안에 있다. 자습본 슬라이드는 이미
새 방법으로 바뀌었으니 데크는 달라지지 않는다. 달라지는 것은 녹화할 때 읽을
말이다. 두 곳이 다른 말을 하면 녹화가 자습본과 어긋난다.

대본은 문장마다 줄을 나누고 네 칸을 들여 쓴다. 그래서 바꾸는 단위도 문단이다.

    python script_relcoord.py [--dry]
"""
import io
import os
import sys
# 저장소 뿌리에서 상대로 잡는다 — 남의 컴퓨터에서도 돌아가야 한다.
REPO = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))


ROOT = os.path.join(REPO, 'projects', 'autocad-technician')

PATCH = [
 # ── 2차시 · 표제란 ──────────────────────────────────────────
 ('lesson-02-part-and-template',
  '    반대 구석점을 물어봅니다. `@-200,30` 입력하고 엔터.',
  '    반대 구석점을 물어봅니다. 여기서는 반대 구석을 찍지 않습니다.\n'
  '    `D` 입력하고 엔터. 치수 옵션이에요. 가로와 세로를 숫자로 주겠다는 뜻입니다.\n'
  '    가로를 물어봅니다. `200` 엔터. 세로를 물어봅니다. `30` 엔터.\n'
  '    이제 방향만 남았어요. 커서를 그 모서리의 왼쪽 위로 옮기면 사각형이 그쪽으로 미리 보입니다.\n'
  '    클릭합니다. 가로 200 세로 30 사각형이 붙었습니다.'),

 # ── 3차시 · 베이스 외곽 ────────────────────────────────────
 ('lesson-03-baseline-profile',
  '''    그리기 전에 상태 막대를 한 번 더 봅니다. 객체 스냅 F3이 켜져 있어야 합니다.
    `PL` 입력하고 엔터. 폴리선 명령입니다.
    시작점을 물어봅니다. 확대해 둔 자리의 왼쪽 아래쯤을 그냥 클릭합니다.
    여기가 베이스의 왼쪽 아래 구석이 됩니다.
    이 점이 화면 어디에 찍히든 상관없어요. 도면 치수가 아니거든요.
    부품이 도면틀 안에 들어갈 자리인지만 봅니다.

    다음 점 `@120,0` 엔터. 오른쪽으로 120. 베이스 폭입니다.
    또는 직교를 켜 두고 커서를 그 점보다 오른쪽에 둔 뒤 `120` 만 쳐도 같은 자리에 찍힙니다.
    다음 점 `@0,11` 엔터. 위로 11.
    이것도 커서를 위에 두고 `11` 로 대신할 수 있어요.

    11은 베이스 높이 16에서 잘라내는 5를 뺀 값입니다.
    오른쪽 세로변은 여기까지만 올라가요.

    명령은 아직 끝나지 않았습니다.
    엔터를 누르지 말고 이어서 갑니다.''',
  '''    그리기 전에 상태 막대를 한 번 더 봅니다. 객체 스냅 F3이 켜져 있어야 합니다.
    `REC` 입력하고 엔터. 사각형 명령입니다.
    첫 번째 구석점을 물어봅니다. 확대해 둔 자리의 왼쪽 아래쯤을 그냥 클릭합니다.
    여기가 베이스의 왼쪽 아래 구석이 됩니다.
    이 점이 화면 어디에 찍히든 상관없어요. 도면 치수가 아니거든요.
    부품이 도면틀 안에 들어갈 자리인지만 봅니다.

    반대 구석점을 물어봅니다. 여기서 반대 구석을 찍지 않습니다.
    `D` 입력하고 엔터. 치수 옵션이에요.
    가로를 물어봅니다. `120` 엔터. 베이스 폭입니다.
    세로를 물어봅니다. `16` 엔터. 정면에서 본 베이스 높이예요.

    이제 방향만 남았습니다. 커서를 그 점의 오른쪽 위로 옮기면 사각형이 그쪽으로 미리 보여요.
    클릭합니다. 120 곱하기 16 사각형이 생겼습니다.

    두 숫자 다 도면에 그대로 적혀 있는 값입니다.
    계산해서 만든 값이 하나도 없어요.
    모따기는 아직 없습니다. 다음 단계에서 따로 냅니다.'''),

 ('lesson-03-baseline-profile',
  '''    다음 점 `@-5,5` 엔터.
    왼쪽으로 5, 위로 5입니다.
    2-C5 중 오른쪽 하나가 그려졌어요.
    가로로 5 세로로 5면 기울기가 정확히 45도입니다.

    이 한 줄만 직교로는 되지 않아요. 대각선이라 수평도 수직도 아닙니다.
    도면에 적힌 5와 5를 그대로 넣는 골뱅이 마이너스 5,5가 가장 짧습니다.

    상대극좌표로도 됩니다. `@7.07<135` 입니다.
    5와 5로 만든 대각선의 길이가 7.07이에요.
    5제곱 더하기 5제곱의 제곱근입니다.
    왼쪽 위 방향이 135도입니다.
    다만 7.07은 반올림한 값이라 아주 미세하게 어긋납니다.
    도면에 적힌 값은 7.07이 아니라 5와 5입니다.
    그래서 여기서는 골뱅이 마이너스 5,5가 맞습니다.
    상대극좌표는 각도와 거리가 도면에 그대로 적혀 있을 때 씁니다.''',
  '''    `CHA` 입력하고 엔터. 모따기 명령입니다.
    `D` 입력하고 엔터. 거리 옵션이에요.
    첫 번째 거리를 물어봅니다. `5` 엔터.
    두 번째 거리를 물어봅니다. 같은 값이라 그냥 엔터를 눌러도 됩니다.

    이제 자를 구석을 이루는 두 변을 차례로 클릭합니다.
    오른쪽 위 구석의 윗변을 클릭하고, 오른쪽 세로변을 클릭합니다.
    구석이 45도로 잘렸어요. 가로 5 세로 5라 정확히 45도입니다.

    도면의 2-C5가 이 값입니다. 5와 5를 그대로 넣었어요.
    사각형을 그리면서 모따기까지 하려면 11이니 110이니 하는 값을 손으로 계산해야 합니다.
    그 값들은 도면 어디에도 없어요.
    사각형을 먼저 그리고 모따기를 따로 내면 도면에 적힌 숫자만 씁니다.'''),

 ('lesson-03-baseline-profile',
  '''    다음 점 `@-110,0` 엔터. 왼쪽으로 110. 베이스 윗면입니다.
    110은 120에서 좌우 모따기 5씩을 뺀 값이에요.
    직교를 켜 두었다면 커서를 왼쪽에 두고 `110` 만 쳐도 돼요.

    다음 점 `@-5,-5` 엔터. 왼쪽 모따기입니다.

    이제 `C` 입력하고 엔터. 닫기 옵션입니다.
    처음 클릭한 그 점까지 자동으로 이어지고 명령이 끝납니다.

    마지막 한 변을 왜 손으로 찍지 않을까요.
    닫기는 끝점을 시작점과 똑같은 자리에 정확히 붙입니다.
    사람이 찍으면 소수점 아래에서 어긋나요.''',
  '''    왼쪽 위 구석도 같은 방법입니다.
    `CHA` 다시 입력하고 엔터. 거리는 그대로 5, 5로 남아 있어요.
    윗변을 클릭하고 왼쪽 세로변을 클릭합니다.

    이제 여섯 변짜리 윤곽이 됐습니다.
    아래 두 구석은 직각 그대로예요. 도면에도 위 두 곳에만 2-C5가 적혀 있습니다.

    네 구석을 한꺼번에 자르는 방법도 있습니다.
    `CHA` 에서 `P` 를 누르고 사각형을 클릭하면 됩니다.
    다만 그러면 아래 두 구석까지 잘려요. 그래서 여기서는 하나씩 집습니다.

    사각형은 처음부터 닫혀 있으니 마지막 변을 손으로 찍을 일도 없습니다.
    사람이 찍으면 소수점 아래에서 어긋나요.'''),

 ('lesson-03-baseline-profile',
  '    `@0,-5` 입력하고 엔터. 기준점에서 아래로 5입니다.',
  '    커서를 그 점보다 아래에 둡니다. 직교가 켜져 있어 아래로 고정돼요.\n'
  '    `5` 입력하고 엔터. 기준점에서 아래로 5입니다.\n'
  '    방향은 커서가, 거리는 숫자가 정합니다. 부호를 칠 일이 없어요.'),

 ('lesson-03-baseline-profile',
  '    `@-33,62` 입력하고 엔터.',
  '    여기서는 잡을 것이 없습니다. 그래서 길이를 먼저 만들고 옮깁니다.\n'
  '    빈 자리를 아무 데나 클릭하고, 직교를 켠 채 커서를 오른쪽으로 끕니다.\n'
  '    `66` 엔터, 다시 엔터. 33의 두 배입니다.\n'
  '    `M` 입력하고 엔터. 이동 명령이에요. 방금 그린 선을 클릭하고 엔터.\n'
  '    기준점을 물어봅니다. 그 선의 한가운데에 커서를 올려 세모 표식이 뜨면 클릭합니다.\n'
  '    두 번째 점에서 `FROM` 입력하고 엔터.\n'
  '    세로 중심선과 베이스 밑변이 만나는 자리를 클릭합니다. 가위표 표식이에요.\n'
  '    커서를 위에 두고 `62` 엔터. 선의 한가운데가 그 자리에 가서 앉습니다.'),

 ('lesson-03-baseline-profile',
  '    `@-40,0` 입력하고 엔터. 왼쪽으로 40, 밑동 80의 절반이에요.',
  '    커서를 그 점의 왼쪽에 둡니다. 직교가 수평으로 잡아 줍니다.\n'
  '    `40` 입력하고 엔터. 밑동 80의 절반이에요.'),

 ('lesson-03-baseline-profile',
  '    `@40,0` 입력하고 엔터. 이번에는 오른쪽으로 40이에요.',
  '    이번에는 커서를 그 점의 오른쪽에 둡니다.\n'
  '    `40` 입력하고 엔터. 같은 값, 반대쪽이에요.'),

 # ── 4차시 ──────────────────────────────────────────────────
 ('lesson-04-circles-arcs',
  '    다음 점을 물어봅니다. `@22<45` 입력하고 엔터.',
  '    다음 점을 물어봅니다. 상태 막대의 극좌표 추적을 켭니다. F10이에요.\n'
  '    각도 증분이 45도인지 확인합니다.\n'
  '    커서를 오른쪽 위로 끕니다. 45도에 닿으면 점선 안내선과 함께 극좌표 45도가 뜹니다.\n'
  '    안내선이 뜬 상태에서 `22` 입력하고 엔터.\n'
  '    방향은 안내선이 잡고 거리만 숫자로 줍니다. 정확히 45도예요.'),

 ('lesson-04-circles-arcs',
  '    `@29,8` 입력하고 엔터. 그 구석에서 오른쪽으로 29, 위로 8입니다.',
  '    그 자리에는 잡을 것이 없습니다. 그래서 잡을 것을 먼저 만듭니다.\n'
  '    `O` 입력하고 엔터. 간격 띄우기입니다. 거리 `8` 엔터.\n'
  '    베이스 밑변을 클릭하고, 그 위쪽을 한 번 더 클릭합니다.\n'
  '    엔터로 명령을 되부르고 거리 `29` 엔터.\n'
  '    베이스 왼쪽 변을 클릭하고, 그 오른쪽을 한 번 더 클릭합니다.\n'
  '    두 보조선이 만나는 자리가 장공 왼쪽 끝원의 중심입니다.\n'
  '    커서를 그 자리에 올려 가위표 표식이 뜨면 클릭합니다.'),

 # ── 5차시 ──────────────────────────────────────────────────
 ('lesson-05-three-views',
  '    클릭한 다음 `@0,-5` 입력하고 엔터.',
  '    클릭한 다음 커서를 그 점보다 아래에 두고 `5` 입력하고 엔터.'),
]

# 5차시의 두 줄은 같은 문장이 두 번 나온다. 한꺼번에 바꾼다.
BULK = [
 ('lesson-05-three-views',
  '    `@-5,0` 엔터. 또는 커서를 왼쪽에 두고 `5` 엔터.',
  '    커서를 그 점의 왼쪽에 두고 `5` 엔터.'),
]


def main(dry=False):
    done, miss = 0, []
    files = {}
    for les, old, new in PATCH:
        p = os.path.join(ROOT, les, 'SCRIPT.md')
        files.setdefault(p, io.open(p, encoding='utf-8').read())
        s = files[p]
        n = s.count(old)
        if n != 1:
            miss.append((les, old.strip()[:56], '%d번 나옴' % n))
            continue
        files[p] = s.replace(old, new, 1)
        done += 1
    for les, old, new in BULK:
        p = os.path.join(ROOT, les, 'SCRIPT.md')
        files.setdefault(p, io.open(p, encoding='utf-8').read())
        n = files[p].count(old)
        if not n:
            miss.append((les, old.strip()[:56], '없음'))
            continue
        files[p] = files[p].replace(old, new)
        done += n

    if not dry:
        for p, s in files.items():
            io.open(p, 'w', encoding='utf-8', newline='\n').write(s)

    print('바꾼 문단 %d개' % done)
    for les, what, why in miss:
        print('  못 바꿈 · %s · %s · %s' % (les[7:9], what, why))
    left = 0
    for p in files:
        import re
        left += len(re.findall(r'@[-\d]', files[p]))
    print('대본에 남은 상대좌표 %d개' % left)
    return 0


if __name__ == '__main__':
    sys.exit(main('--dry' in sys.argv))
